// 现有导航局部加载与对账直接承接入口的独立浏览器回归。
const {chromium}=require(process.env.CAESAR_PLAYWRIGHT_PATH||'/tmp/wechat-style-tools/node_modules/playwright-core');const assert=require('node:assert/strict');const {pathToFileURL}=require('node:url');
(async()=>{const b=await chromium.launch({executablePath:process.env.CAESAR_CHROME_PATH||'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome',headless:true,args:['--allow-file-access-from-files']});const p=await b.newPage({viewport:{width:1440,height:1000}});const errors=[];p.on('pageerror',e=>errors.push(e.message));try{await p.goto(pathToFileURL(require('node:path').resolve(__dirname,'../merchant/finance/finance-supplier-bills.html')).href);await p.locator('.nav-primary-item[data-title="履约中心"]').click();await p.locator('.nav-scroll a[href$="tour/fulfillment-cost.html"]').click();await p.locator('[data-settlement-work-tab="reconciliation"]').click();await p.locator('[data-rec-action="create"]').click();await p.locator('[data-rec-select="C04"]').check();assert.ok((await p.locator('#recSelectionTotal').innerText()).includes('6,000.00'));console.log('PASS 菜单局部加载后对账脚本可用，选中费用保留预付冲抵6000');await p.locator('[data-rec-action="submit"]').click();assert.equal(await p.locator('.rec-list [data-rec-id]').count(),8);console.log('PASS 菜单进入后可提交对账');await p.locator('[data-rec-open="DZ20260926003"]').click();await p.locator('.rec-related a').filter({hasText:'付款申请'}).click();await p.waitForTimeout(500);assert.equal(await p.locator('#paymentWorkTabs').isVisible(),true);assert.equal(await p.locator('#settlementWorkTabs').isHidden(),true);console.log('PASS 对账详情付款入口仍进入付款申请模式');assert.deepEqual(errors,[]);console.log('完成3组菜单及承接检查，脚本异常0');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1});
