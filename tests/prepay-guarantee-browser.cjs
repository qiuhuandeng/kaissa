const assert=require('node:assert/strict'),fs=require('node:fs/promises'),path=require('node:path'),http=require('node:http');
const {pathToFileURL}=require('node:url'),{chromium}=require('playwright');
(async()=>{
 const root=path.resolve(__dirname,'..'),out='/private/tmp/caesar-prepay-guarantee';await fs.mkdir(out,{recursive:true});
 const server=http.createServer(async(req,res)=>{try{const p=path.resolve(root,'.'+decodeURIComponent(new URL(req.url,'http://localhost').pathname));if(!p.startsWith(root+path.sep))throw Error();res.setHeader('Content-Type',({'.js':'text/javascript','.css':'text/css','.html':'text/html','.svg':'image/svg+xml'})[path.extname(p)]||'application/octet-stream');res.end(await fs.readFile(p));}catch{res.writeHead(404);res.end();}});
 await new Promise(r=>server.listen(0,'127.0.0.1',r));const browser=await chromium.launch({channel:'chrome',headless:true}),errors=[],checks=[];
 try{for(const protocol of ['file','http']){
 const page=await browser.newPage({viewport:{width:1440,height:1000},acceptDownloads:true});page.on('pageerror',e=>errors.push(e.message));
 const url=protocol==='file'?pathToFileURL(path.join(root,'merchant/data/prepayment-reports.html')).href:'http://127.0.0.1:'+server.address().port+'/merchant/data/prepayment-reports.html';

 await page.goto(url);let host;const tab=async key=>{await page.locator('[data-report-tab="'+key+'"]').click();host=page.locator(key==='prepay'?'#finance-prepayments':'#finance-guarantee-balance');},field=n=>host.locator('[name="'+n+'"]'),submit=()=>host.locator('[type=submit]').click();
 const csv=async()=>{const pending=page.waitForEvent('download');await host.locator('[data-fr-export]').click();const d=await pending,p=path.join(out,protocol+'-'+d.suggestedFilename());await d.saveAs(p);return fs.readFile(p,'utf8');};
 await tab('prepay');assert.equal(await page.locator('table:visible').count(),1);assert.equal(await host.locator('[data-fr-content]').isVisible(),false);await host.locator('.cf-more>summary').click();await field('keyword').fill('YF-001');await submit();assert.match(await host.locator('tbody').innerText(),/230,000.00/);await host.locator('.cf-columns>summary').click();await host.locator('[data-fr-column=allocated]').check();await host.locator('[data-fr-column=unallocated]').check();assert.match(await host.locator('tbody').innerText(),/200,000.00/);assert.match(await host.locator('tbody').innerText(),/未提供/);let data=await csv();assert.doesNotMatch(data,/FT-A|完整历史变动|本期增加/);checks.push(protocol+': 预付单表及累计分摊不推剩余');
 await field('keyword').fill('未提交');await tab('guarantee');assert.equal(await page.locator('table:visible').count(),1);assert.equal(await host.locator('tbody tr').count(),2);await field('direction').selectOption('收取');await submit();assert.equal(await host.locator('tbody tr').count(),1);assert.match(await host.locator('tbody').innerText(),/30,000.00/);await tab('prepay');assert.equal(await field('keyword').inputValue(),'未提交');assert.match(await host.locator('tbody').innerText(),/230,000.00/);await page.reload();assert.equal(await field('keyword').inputValue(),'未提交');checks.push(protocol+': 保证金方向及独立草稿刷新');
 await host.locator('[data-fr-reset]').click();await field('end').fill('2026-08-01');await submit();assert.equal(await host.locator('tbody tr').count(),0);assert.equal(await host.locator('[data-fr-export]').isDisabled(),true);await host.locator('[data-fr-reset]').click();data=await csv();assert.match(data,/YF-PAGE-25/);assert.match(data,/未提供/);checks.push(protocol+': 日期错误及完整跨页导出');
 for(const key of ['prepay','guarantee']){await tab(key);await host.locator('[data-fr-reset]').click();for(const width of [1440,768]){await page.setViewportSize({width,height:1000});assert.equal(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth),true);await host.locator('[data-fr-main]').evaluate(el=>el.scrollLeft=el.scrollWidth);assert.equal(await host.locator('tbody td').evaluateAll(cs=>cs.every(c=>getComputedStyle(c).textAlign==='left'&&c.scrollWidth<=c.clientWidth+1)),true);await host.locator('[data-fr-main]').evaluate(el=>el.scrollLeft=0);await page.screenshot({path:path.join(out,protocol+'-'+key+'-'+width+'.png'),fullPage:true});}assert.equal(await host.locator('tbody a,tbody button').count(),0);}
 for(const key of ['advance','deposit']){await page.locator('[data-report-tab="'+key+'"]').click();assert.equal(await page.locator('table:visible').count(),1);}checks.push(protocol+': 桌面窄屏只读及预收预存回归');await page.close();
 }assert.deepEqual(errors,[]);await fs.writeFile(path.join(out,'results.json'),JSON.stringify({checks,errors},null,2));console.log(JSON.stringify({checks,errors}));}
 finally{await browser.close();server.close();}
})().catch(e=>{console.error(e);process.exitCode=1});
