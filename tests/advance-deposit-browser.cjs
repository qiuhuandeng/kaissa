const assert=require('node:assert/strict'),fs=require('node:fs/promises'),path=require('node:path'),http=require('node:http');
const {pathToFileURL}=require('node:url'),{chromium}=require('playwright');
(async()=>{
 const root=path.resolve(__dirname,'..'),out='/private/tmp/caesar-advance-deposit';await fs.mkdir(out,{recursive:true});
 const server=http.createServer(async(req,res)=>{try{const p=path.resolve(root,'.'+decodeURIComponent(new URL(req.url,'http://localhost').pathname));if(!p.startsWith(root+path.sep))throw Error();res.setHeader('Content-Type',({'.js':'text/javascript','.css':'text/css','.html':'text/html','.svg':'image/svg+xml'})[path.extname(p)]||'application/octet-stream');res.end(await fs.readFile(p));}catch{res.writeHead(404);res.end();}});
 await new Promise(r=>server.listen(0,'127.0.0.1',r));const browser=await chromium.launch({channel:'chrome',headless:true}),errors=[],checks=[];
 try{for(const protocol of ['file','http']){
 const page=await browser.newPage({viewport:{width:1440,height:1000},acceptDownloads:true});page.on('pageerror',e=>errors.push(e.message));
 const url=protocol==='file'?pathToFileURL(path.join(root,'merchant/data/prepayment-reports.html')).href:'http://127.0.0.1:'+server.address().port+'/merchant/data/prepayment-reports.html';

 await page.goto(url);let host;const tab=async key=>{await page.locator('[data-report-tab="'+key+'"]').click();host=page.locator('#finance-'+key+'-balance');},field=n=>host.locator('[name="'+n+'"]'),submit=()=>host.locator('[type=submit]').click();
 const csv=async()=>{const pending=page.waitForEvent('download');await host.locator('[data-fr-export]').click();const d=await pending,p=path.join(out,protocol+'-'+d.suggestedFilename());await d.saveAs(p);return fs.readFile(p,'utf8');};
 await tab('advance');assert.equal(await page.locator('table:visible').count(),1);assert.match(await host.locator('tbody').innerText(),/8,000.00/);assert.match(await host.locator('tbody').innerText(),/6,000.00/);await host.locator('[data-fr-content]').selectOption('items');assert.match(await host.locator('tbody').innerText(),/YS-001/);let data=await csv();assert.match(data,/YS-001/);assert.doesNotMatch(data,/SK-001|本期增加|YF-001/);checks.push(protocol+': 预收汇总组成单表及导出不混流水');
 await field('party').fill('未查询');await tab('deposit');assert.equal(await page.locator('table:visible').count(),1);assert.equal(await host.locator('[data-fr-content]').isVisible(),false);assert.match(await host.locator('tbody').innerText(),/24,000.00/);assert.match(await host.locator('tbody').innerText(),/21,000.00/);assert.match(await host.locator('tbody').innerText(),/未提供/);await tab('advance');assert.equal(await field('party').inputValue(),'未查询');assert.match(await host.locator('tbody').innerText(),/YS-001/);await page.reload();assert.equal(await field('party').inputValue(),'未查询');assert.match(await host.locator('tbody').innerText(),/YS-001/);checks.push(protocol+': 预存账户无无效切换及独立草稿');
 await host.locator('[data-fr-reset]').click();await field('end').fill('2026-08-01');await submit();assert.equal(await host.locator('tbody tr').count(),0);assert.equal(await host.locator('[data-fr-export]').isDisabled(),true);await host.locator('[data-fr-reset]').click();checks.push(protocol+': 日期错误清空及禁导出');
 for(const key of ['advance','deposit']){await tab(key);await host.locator('[data-fr-reset]').click();for(const width of [1440,768]){await page.setViewportSize({width,height:1000});assert.equal(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth),true);await host.locator('[data-fr-main]').evaluate(el=>el.scrollLeft=el.scrollWidth);assert.equal(await host.locator('tbody td').evaluateAll(cs=>cs.every(c=>getComputedStyle(c).textAlign==='left'&&c.scrollWidth<=c.clientWidth+1)),true);await host.locator('[data-fr-main]').evaluate(el=>el.scrollLeft=0);await page.screenshot({path:path.join(out,protocol+'-'+key+'-'+width+'.png'),fullPage:true});}assert.equal(await host.locator('tbody a,tbody button').count(),0);}
 for(const key of ['prepay','guarantee']){await page.locator('[data-report-tab="'+key+'"]').click();const target=page.locator(key==='prepay'?'#finance-prepayments':'#finance-guarantee-balance');assert.equal(await target.isVisible(),true);assert.ok(await target.locator('tbody tr').count()>0);}checks.push(protocol+': 桌面窄屏只读及其他两主题回归');await page.close();
 }assert.deepEqual(errors,[]);await fs.writeFile(path.join(out,'results.json'),JSON.stringify({checks,errors},null,2));console.log(JSON.stringify({checks,errors}));}
 finally{await browser.close();server.close();}
})().catch(e=>{console.error(e);process.exitCode=1});
