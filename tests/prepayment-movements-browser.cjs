const assert=require('node:assert/strict'),fs=require('node:fs/promises'),path=require('node:path'),http=require('node:http');
const {pathToFileURL}=require('node:url'),{chromium}=require('playwright');
(async()=>{
 const root=path.resolve(__dirname,'..'),out='/private/tmp/caesar-prepayment-movements';await fs.mkdir(out,{recursive:true});
 const server=http.createServer(async(req,res)=>{try{const p=path.resolve(root,'.'+decodeURIComponent(new URL(req.url,'http://localhost').pathname));if(!p.startsWith(root+path.sep))throw Error();res.setHeader('Content-Type',({'.js':'text/javascript','.css':'text/css','.html':'text/html','.svg':'image/svg+xml'})[path.extname(p)]||'application/octet-stream');res.end(await fs.readFile(p));}catch{res.writeHead(404);res.end();}});
 await new Promise(r=>server.listen(0,'127.0.0.1',r));const browser=await chromium.launch({channel:'chrome',headless:true}),errors=[],checks=[];
 try{for(const protocol of ['file','http']){
 const page=await browser.newPage({viewport:{width:1440,height:1000},acceptDownloads:true});page.on('pageerror',e=>errors.push(e.message));
 const url=protocol==='file'?pathToFileURL(path.join(root,'merchant/data/prepayment-reports.html')).href:'http://127.0.0.1:'+server.address().port+'/merchant/data/prepayment-reports.html';

 await page.goto(url);await page.locator('[data-report-tab=movements]').click();const host=page.locator('#finance-prepayment-movements'),field=n=>host.locator('[name="'+n+'"]'),submit=()=>host.locator('[type=submit]').click();
 assert.equal(await page.locator('[data-report-tab]').count(),5);assert.equal(await page.locator('table:visible').count(),1);assert.match(await host.locator('tbody').innerText(),/期初余额/);assert.match(await host.locator('tbody').innerText(),/期末余额/);assert.match(await host.locator('tbody').innerText(),/6,000.00/);assert.equal(await host.locator('[data-fr-content]').isVisible(),false);checks.push(protocol+': 五主题独立及账户期初期间期末');
 await field('category').selectOption('prepay');await host.locator('.cf-more>summary').click();await field('keyword').fill('YF-001');await submit();assert.match(await host.locator('tbody').innerText(),/300,000.00/);const pending=page.waitForEvent('download');await host.locator('[data-fr-export]').click();const d=await pending,p=path.join(out,protocol+'-'+d.suggestedFilename());await d.saveAs(p);const csv=await fs.readFile(p,'utf8');assert.match(csv,/230000/);assert.doesNotMatch(csv,/TH-002|YC-001/);checks.push(protocol+': 分摊不扣账面及同范围完整导出');
 await field('keyword').fill('未提交');await page.locator('[data-report-tab=advance]').click();await page.locator('[data-report-tab=movements]').click();assert.equal(await field('keyword').inputValue(),'未提交');await page.reload();assert.equal(await field('keyword').inputValue(),'未提交');assert.match(await host.locator('tbody').innerText(),/300,000.00/);await host.locator('[data-fr-reset]').click();await field('start').fill('2026-10-01');await submit();assert.equal(await host.locator('tbody tr').count(),0);assert.equal(await host.locator('[data-fr-export]').isDisabled(),true);await host.locator('[data-fr-reset]').click();checks.push(protocol+': 草稿刷新独立及错误禁导出');
 for(const width of [1440,768]){await page.setViewportSize({width,height:1000});assert.equal(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth),true);await host.locator('[data-fr-main]').evaluate(el=>el.scrollLeft=el.scrollWidth);assert.equal(await host.locator('tbody td').evaluateAll(cs=>cs.every(c=>getComputedStyle(c).textAlign==='left'&&c.scrollWidth<=c.clientWidth+1)),true);await host.locator('[data-fr-main]').evaluate(el=>el.scrollLeft=0);await page.screenshot({path:path.join(out,protocol+'-'+width+'.png'),fullPage:true});}assert.equal(await host.locator('tbody a,tbody button').count(),0);for(const key of ['advance','deposit','prepay','guarantee']){await page.locator('[data-report-tab="'+key+'"]').click();assert.equal(await page.locator('table:visible').count(),1);}checks.push(protocol+': 桌面窄屏只读及原四主题回归');await page.close();
 }assert.deepEqual(errors,[]);await fs.writeFile(path.join(out,'results.json'),JSON.stringify({checks,errors},null,2));console.log(JSON.stringify({checks,errors}));}
 finally{await browser.close();server.close();}
})().catch(e=>{console.error(e);process.exitCode=1});
