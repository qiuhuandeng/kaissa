/* 复用合同模板列表、编辑及通知模板入口；供应商用途独立于销售合同。 */
(function(){
  'use strict';
  const T=window.SupplierContractTools, q=new URLSearchParams(location.search);
  const e=v=>String(v==null?'':v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const $=id=>document.getElementById(id);
  const button=(label,act,primary=false)=>'<button type="button" class="btn '+(primary?'btn-primary':'btn-secondary')+'" data-st-action="'+act+'">'+label+'</button>';
  const field=(label,id,value='',type='text')=>'<div class="supplier-field"><label for="'+id+'">'+label+'</label><input class="form-control" id="'+id+'" type="'+type+'" value="'+e(value)+'"></div>';
  const checks=(label,name,values,selected)=>'<fieldset class="supplier-check-field supplier-full"><legend>'+label+'</legend><div class="supplier-checks">'+values.map(v=>'<label><input type="checkbox" name="'+name+'" value="'+e(v)+'"'+(selected.includes(v)?' checked':'')+'> '+e(v)+'</label>').join('')+'</div></fieldset>';
  const values=name=>Array.from(document.querySelectorAll('[name="'+name+'"]:checked')).map(x=>x.value);
  const section=(title,html)=>'<section class="supplier-form-section"><div class="drawer-section-head"><h3 class="drawer-section-title">'+title+'</h3></div>'+html+'</section>';
  const switcher='<nav class="supplier-purpose-nav" aria-label="合同用途"><a href="contract-templates.html"'+(q.get('purpose')!=='supplier'?' aria-current="page"':'')+'>游客／客户合同</a><a href="contract-templates.html?purpose=supplier"'+(q.get('purpose')==='supplier'?' aria-current="page"':'')+'>供应商合作协议</a></nav>';
  let draft,dirty=false,noticeMode=false;
  function layer(title,content){$('stTitle').textContent=title;$('stContent').innerHTML=content;window.caesarUI.openLayer($('stLayer'));}
  function close(){if(noticeMode&&dirty){ask(()=>{dirty=false;window.caesarUI.closeLayer($('stLayer'));});return;}window.caesarUI.closeLayer($('stLayer'));}
  function ask(fn){$('stConfirm').hidden=false;window.caesarUI.openLayer($('stConfirm'));$('stLeave').onclick=()=>{window.caesarUI.closeLayer($('stConfirm'));fn();};}
  function error(text){const el=$('stError');if(el){el.hidden=false;el.textContent=text;el.focus();}else window.caesarUI.toast(text);}
  function setupLayers(){document.body.insertAdjacentHTML('beforeend','<div id="stLayer" class="modal-overlay drawer-overlay" data-static="true" hidden aria-hidden="true"><section class="modal drawer-modal drawer-lg supplier-drawer" role="dialog" aria-modal="true" aria-labelledby="stTitle"><div class="modal-header"><h2 class="modal-title" id="stTitle"></h2><button class="modal-close" data-st-action="close" aria-label="关闭">×</button></div><div class="modal-body" id="stContent"></div><div class="modal-footer" id="stFooter">'+button('关闭','close')+'</div></section></div><div class="modal-overlay" id="stConfirm" data-static="true" hidden aria-hidden="true"><section class="modal modal-sm modal-confirm" role="dialog" aria-modal="true"><div class="modal-header"><h2 class="modal-title">放弃未保存的修改？</h2></div><div class="modal-body">当前内容未保存，确认离开吗？</div><div class="modal-footer">'+button('取消','stay')+'<button id="stLeave" class="btn btn-primary">确认离开</button></div></section></div>');}
  function list(){
    const main=document.getElementById('supplierTemplateHost');main.className='supplier-template-page';
    main.innerHTML=switcher+'<section class="list-surface" data-list-surface><section class="filter-card filter-card-compact list-surface-filter"><div class="list-surface-filter-actions list-filter-pinned-actions"><a class="btn btn-primary" href="contract-template-edit.html?purpose=supplier&mode=create">新增模板</a></div><div class="filter-row"><div class="filter-item"><input id="stSearch" aria-label="模板名称或版本" placeholder="模板名称／版本"></div><div class="filter-item filter-item-sm"><select id="stCategory" aria-label="供应商类型"><option value="">全部供应商类型</option>'+T.categories.map(c=>'<option>'+c+'</option>').join('')+'</select></div><div class="filter-item filter-item-sm"><select id="stState" aria-label="模板状态"><option value="">全部状态</option><option>启用</option><option>待审核</option><option>停用</option></select></div><div class="filter-actions">'+button('搜索','search')+button('重置','reset')+'</div></div></section><div class="table-wrap supplier-template-table"><table><colgroup><col><col class="st-type-col"><col class="st-company-col"><col class="st-state-col"><col class="st-owner-col"><col class="st-action-col"></colgroup><thead><tr><th>模板名称／版本</th><th>供应商类型</th><th>适用签约公司</th><th>状态</th><th>维护人</th><th class="st-sticky">操作</th></tr></thead><tbody id="stRows"></tbody></table></div><div class="pagination" id="stCount"></div></section>';
    rows();
  }
  function rows(){const filtered=T.templates.filter(t=>(!$('stSearch').value||[t.name,t.version,t.id].join(' ').includes($('stSearch').value.trim()))&&(!$('stCategory').value||t.categories.includes($('stCategory').value))&&(!$('stState').value||t.status===$('stState').value));$('stRows').innerHTML=filtered.map(t=>'<tr><td><div class="supplier-cell"><strong>'+e(t.name)+'</strong><small>'+e(t.version)+'</small></div></td><td>'+e(t.categories.join('、'))+'</td><td>'+e(t.companies.join('、'))+'</td><td><span class="tag '+(t.status==='启用'?'tag-green':t.status==='待审核'?'tag-orange':'tag-gray')+'">'+e(t.status)+'</span></td><td>'+e(t.owner)+'</td><td class="st-sticky"><div class="table-action"><button class="table-action-primary" data-st-view="'+t.id+'">查看</button><a href="contract-template-edit.html?purpose=supplier&template='+t.id+'">'+(t.status==='待审核'?'详情':'修订')+'</a></div></td></tr>').join('')||'<tr><td colspan="6" class="supplier-empty">没有符合条件的模板</td></tr>';$('stCount').textContent='共'+filtered.length+'条';}
  function readEditor(){draft.name=$('stName').value.trim();draft.version=$('stVersion').value.trim();draft.categories=values('stCategories');draft.companies=values('stCompanies');draft.text=$('stText').value;return draft;}
  function editor(){
    const source=T.templates.find(t=>t.id===q.get('template'));
    if(q.get('template')&&!source){document.getElementById('supplierTemplateHost').innerHTML='<h1 class="page-title">未找到供应商模板</h1><a href="contract-templates.html?purpose=supplier">返回</a>';return;}
    draft=source?T.copy(source):{id:'SUP-NEW',name:'',version:'V1.0',status:'草稿',categories:[],companies:[],text:T.defaultText,owner:'采购／法务'};
    const locked=draft.status==='待审核';if(source&&!locked){draft.parentId=source.id;draft.parentVersion=source.version;draft.version=source.version+'-修订1';draft.status='草稿';}
    const main=document.getElementById('supplierTemplateHost');main.className='supplier-template-page supplier-template-editor';
    main.innerHTML='<section class="page-workbar"><div class="page-title-row"><a class="page-back-link" data-st-back href="contract-templates.html?purpose=supplier">返回</a><h1 class="page-title">供应商协议模板</h1></div><div class="btn-group">'+button('预览','preview')+(!locked?button('保存草稿','save')+button('提交审核','submit',true):'')+'</div></section><section class="supplier-identity"><div><h2>'+e(source?source.name:'新增供应商协议模板')+'</h2><span class="tag tag-orange" id="stVersionState">'+e(draft.status)+'</span></div><p>'+e(source?'原版本 '+source.version+' · '+source.status:'供应商合作协议')+'</p></section><section class="supplier-card supplier-template-form"><p id="stError" class="supplier-form-error" tabindex="-1" role="alert" hidden></p><fieldset id="stFields"'+(locked?' disabled':'')+'><div class="supplier-form-grid">'+field('模板名称 *','stName',draft.name)+field('版本 *','stVersion',draft.version)+checks('适用供应商类型 *','stCategories',T.categories,draft.categories)+checks('适用签约公司 *','stCompanies',T.companies,draft.companies)+'</div>'+section('合同正文','<div class="supplier-template-variables">'+T.variables.map(v=>'<button type="button" class="btn btn-secondary" data-st-variable="'+v+'">'+v+'</button>').join('')+'</div><label for="stText" class="supplier-field-note">选择字段插入正文；各签约公司约定包含账期、币种、联系人和平台使用费。</label><textarea id="stText" class="form-control supplier-template-text" rows="18">'+e(draft.text)+'</textarea>')+'</fieldset><div id="stSaved" class="supplier-field-note">'+(locked?'该版本正在审核，暂不可编辑。':'')+'</div></section>';
    main.addEventListener('input',()=>dirty=true);main.addEventListener('change',()=>dirty=true);
  }
  function notices(){
    noticeMode=true;const tbody=document.querySelector('main tbody');
    tbody.insertAdjacentHTML('beforeend','<tr id="supplierNoticeRow"><td>供应商协议临期提醒</td><td>供应商协议</td><td>站内信、企微</td><td>采购／法务</td><td><span class="tag tag-green">启用</span></td><td><div class="table-action"><button class="table-action-primary" data-st-action="notice-edit" data-drawer-title="编辑通知模板">编辑</button></div></td></tr>');
    document.querySelector('.pagination span').textContent='第1–3条／共3条';
    const filter=()=>{const kw=$('noticeTemplateKeyword').value.trim(),channel=$('noticeTemplateChannel').value,status=$('noticeTemplateStatus').value;let count=0;Array.from(tbody.rows).forEach(row=>{row.hidden=!!((kw&&!row.textContent.includes(kw))||(channel!=='全部渠道'&&!row.cells[2].textContent.includes(channel))||(status!=='全部状态'&&!row.cells[4].textContent.includes(status)));if(!row.hidden)count++;});document.querySelector('.pagination span').textContent='共'+count+'条';};
    ['noticeTemplateKeyword','noticeTemplateChannel','noticeTemplateStatus'].forEach(id=>{$(id).addEventListener('input',filter);$(id).addEventListener('change',filter);});
    const controls=document.querySelectorAll('.filter-actions button');controls[0].addEventListener('click',filter);controls[1].addEventListener('click',()=>{$('noticeTemplateKeyword').value='';$('noticeTemplateChannel').selectedIndex=0;$('noticeTemplateStatus').selectedIndex=0;filter();});
  }
  function noticeEdit(){draft=T.copy(T.notice);dirty=false;$('stFooter').innerHTML=button('取消','close')+button('预览','notice-preview')+button('保存','notice-save',true);layer('编辑通知模板','<p id="stError" class="supplier-form-error" hidden tabindex="-1" role="alert"></p>'+section('通知内容','<div class="supplier-form-grid">'+field('模板名称 *','stName',draft.name)+field('版本 *','stVersion',draft.version)+checks('可用渠道 *','stChannels',['站内信','企业微信'],draft.channels)+'</div><div class="supplier-template-variables">'+T.noticeVariables.map(v=>'<button class="btn btn-secondary" data-st-variable="'+v+'">'+v+'</button>').join('')+'</div><label for="stText">通知正文 *</label><textarea id="stText" class="form-control" rows="6">'+e(draft.text)+'</textarea>')+section('渠道状态','<p>企业微信：未接通</p><p>提前天数和接收人在各供应商协议中设置。</p>')+'<div id="stNoticePreview"></div>');$('stContent').oninput=()=>dirty=true;$('stContent').onchange=()=>dirty=true;}
  function noticeRead(){draft.name=$('stName').value.trim();draft.version=$('stVersion').value.trim();draft.channels=values('stChannels');draft.text=$('stText').value;const problem=!draft.name||!draft.version?'请填写模板名称和版本。':!draft.channels.length?'请选择可用渠道。':T.validateText(draft.text,T.noticeVariables,T.noticeVariables);if(problem)throw Error(problem);}
  function init(){
    const path=location.pathname;
    if(path.endsWith('contract-templates.html')&&q.get('purpose')!=='supplier'){document.getElementById('supplierTemplateHost').insertAdjacentHTML('afterbegin',switcher);return;}
    if(path.endsWith('contract-template-edit.html')&&q.get('purpose')!=='supplier')return;
    setupLayers();document.addEventListener('click',ev=>{if(ev.target.closest('#stLayer .modal-close')){ev.preventDefault();ev.stopImmediatePropagation();close();}},true);if(path.endsWith('notice-templates.html'))notices();else if(path.endsWith('contract-template-edit.html'))editor();else list();
    document.addEventListener('click',ev=>{const back=ev.target.closest('[data-st-back]');if(back&&dirty){ev.preventDefault();ask(()=>{dirty=false;location.href=back.href;});return;}
      const variable=ev.target.closest('[data-st-variable]');if(variable){const area=$('stText');area.setRangeText('{{'+variable.dataset.stVariable+'}}',area.selectionStart,area.selectionEnd,'end');area.focus();dirty=true;return;}
      const view=ev.target.closest('[data-st-view]');if(view){const t=T.templates.find(x=>x.id===view.dataset.stView);layer('查看模板',section(e(t.name)+' · '+e(t.version),'<p>适用供应商：'+e(t.categories.join('、'))+'</p><p>签约公司：'+e(t.companies.join('、'))+'</p><pre class="supplier-contract-text">'+e(t.text)+'</pre>'));return;}
      const node=ev.target.closest('[data-st-action]');if(!node)return;
      try{switch(node.dataset.stAction){
        case 'search':rows();break;case 'reset':$('stSearch').value='';$('stCategory').value='';$('stState').value='';rows();break;
        case 'close':close();break;case 'stay':window.caesarUI.closeLayer($('stConfirm'));break;
        case 'preview':{readEditor();const problem=T.validateTemplate(draft);if(problem)throw Error(problem);layer('预览模板',section(e(draft.name)+' · '+e(draft.version),'<pre class="supplier-contract-text">'+e(draft.text)+'</pre>'));break;}
        case 'save':case 'submit':{readEditor();const problem=node.dataset.stAction==='submit'?T.validateTemplate(draft):!draft.name?'请填写模板名称。':'';if(problem)throw Error(problem);if(draft.parentVersion===draft.version)throw Error('修订请使用不同于原版本的版本号。');dirty=false;draft.status=node.dataset.stAction==='submit'?'待审核':'草稿';$('stVersionState').textContent=draft.status;$('stError').hidden=true;$('stSaved').textContent=draft.status==='待审核'?'本次修订已提交审核，原启用版本保留。':'草稿已保存。';if(draft.status==='待审核'){$('stFields').disabled=true;document.querySelectorAll('[data-st-action="save"],[data-st-action="submit"]').forEach(x=>x.hidden=true);}window.caesarUI.toast($('stSaved').textContent);break;}
        case 'notice-edit':noticeEdit();break;
        case 'notice-preview':noticeRead();$('stNoticePreview').innerHTML=section('通知预览','<pre class="supplier-contract-text">'+e(draft.text.replace(/\{\{([^{}]+)\}\}/g,(_,key)=>({'我方签约主体':'福建凯撒','供应商全称':'深圳新启航国际旅行社有限公司','协议名称':'东南亚年度合作协议','协议编号':'AGR-NQ-FJ-001','截止日期':'2026-12-31','剩余天数':'7'}[key])))+'</pre><p class="supplier-field-note">仅预览，未发送。</p>');break;
        case 'notice-save':noticeRead();Object.assign(T.notice,T.copy(draft));dirty=false;const row=$('supplierNoticeRow');row.cells[0].textContent=draft.name;row.cells[2].textContent=draft.channels.map(c=>c==='企业微信'?'企微':c).join('、');window.caesarUI.closeLayer($('stLayer'));window.caesarUI.toast('通知模板已保存。');break;
      }}catch(err){error(err.message);}
    });
    window.addEventListener('beforeunload',ev=>{if(dirty){ev.preventDefault();ev.returnValue='';}});
  }
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();
