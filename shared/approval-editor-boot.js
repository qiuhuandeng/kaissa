/* Last deferred script: all editor dependencies and the document are ready. */
(function () {
  const host = document.getElementById('approvalEditor');
  if (host) window.ApprovalConfigUI.mountEditor(host);
})();
