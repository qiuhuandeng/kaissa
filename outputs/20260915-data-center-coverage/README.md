# 数据中心覆盖复核证据

2026-09-15，从项目根目录打开16个页面、依次切换88个Tab采集。默认视口1440×1000；每个Tab一张可见主表，页面脚本错误0。已与 shared/report-section-tabs.js 的16入口88Tab逐项及顺序比较一致。

- current-pages.json：当前默认表头、行数及可见选择项；不包含原始财务附件。
- audit.cjs：复查脚本，读取现有浏览器测试的页面列表，使用 Playwright 和本机 Chrome；从项目根目录运行，结果写到本目录。需另用导航配置核对测试列表完整性。

本记录只验默认结构，不替代全部计算、可选字段、交互、权限、导出及实账验收。临时截图不作为财务正确性证据。
