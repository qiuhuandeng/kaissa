# 业务页面 Codex 逐条执行指令（按现有菜单）

日期：2026-09-17。交付性质：执行指令及逐条状态登记；P01、P02、P03、P04-A、P08-B、P16已有实施成果，其余按状态执行。2026-09-18用户另指定88-05并已完成定价、附加项目、佣金规则及价格审批；随后完成88-07渠道名额、占位和原渠道释放，见[88-07实施与验收](88-07渠道名额占位原渠道释放实施与验收-20260918.md)。88-07同时完成P08-B，并完成P10-A中的核03资源结果范围，不据此把P10-A其他范围标完成。

主依据：[业务详细修改方案-20260917](业务详细修改方案-20260917.md)。覆盖原 P01—P26、核01—核14，没有增加新核心需求。本文件把同页中能独立完成的处理区拆开，共 **35条指令**；其中 P01、P02、P03、P16 已完成，只保留核验，P22先核验再按缺口修正。指令数量不是新增批次或新增页面数量。

## 使用方式

**找到要执行的菜单，完整复制该条 `text` 代码块，粘贴给 Codex 即可。每次复制一条，不需要再拼接通用前缀。** 代码块内已经包含文件、现状定位、角色、修改内容、验收和收尾要求；收到单条指令即实施该范围，不再先输出一轮空泛方案。

页面编号沿主方案；同页的 A／B／C／D 是不同处理区，执行后保留前面已经完成的部分。阅读排列按菜单，执行可按下方推荐业务顺序；前序尚未改完时允许用一致的独立样例完成当前页，不把跨页实时联动当作验收要求。

## 本次边界与事实

- 以现有原型基本满足业务为前提，只补核心场景缺失、错误业务结果、角色交接不通、必要业财依据缺失。保持已有正确入口、表单、状态和交互，不把整个页面重写成新系统。
- 排版美化、文案润色、低频便利、无关字段补充、真实银行／签约／NC接口、自动同步、生产级权限均不纳入。必要控件沿用共享规范，不能以保留UI为由保留错误金额或状态。
- 供应商列表按用户回撤结果保持；独立团期订单、数据中心报表、门店专项不在本次增改范围。定价、佣金与审批已在后续88-05中完善原入口，未新增菜单或重复配置；退改继续引用现有能力。
- 2026-09-17已静态核对现行菜单、主方案及下列指令中的代码定位；当前源码有工作区未提交变更，后续执行先核实最新内容，不能用覆盖整文件的方式丢掉既有工作。
- 两个补充附件各244行且SHA-256一致（`7fdd2b5f0f6189e501f2bd9351d2e7e777e0c6a595fe169ca7318dde2db31245`），是同一份材料的两次上传，不算两份独立证据。仅在主方案的付款、退款、转款等责任不清处查阅；不从其中追加接口、报表或行业建议。
- 附件中的自动审批、自动负应付、收入按实收确认、固定汇率及跨公司处理等表述不能覆盖主方案的待确认边界。文件中写“已批准／已核实样例”只表示原型给定事实，不是替财务批准制度。

## 推荐执行顺序

以下是便于串起业务的顺序，不要求一次全部执行；每条仍只改自己的页面范围。

| 工作线 | 推荐顺序 | 完成后核对的业务结果 |
|---|---|---|
| 外采确认（承接当前断点） | P08-A → P13 → P09-B → P10-A | 参考余位待确认，补差由我方核价及客户决定，无位有改期取消去向 |
| 混合签证 | P03 → P09-A → P06 | 一单3人仅2人代办，自办不产生送签缺口 |
| 原渠道退位、已售团变更 | P08-B → P04-A → P04-B → P10-A核验 | 原渠道归还、核心变更保留原团并承接售后 |
| 采购减量 | P05 | 零数量、多规格、分配冲突与付款依据正确 |
| 价格与合同 | P08-C → P09-C → P11 | 优惠不重扣，未批不改价，多份合同不多算订单 |
| 对账差异 | P15 → P07-A → P14 | 挂接不清差，批准调整及前次异议保留 |
| 收退与结算 | P07-B → P09-C核验 → P10-B → P20 → P21 → P22 | 应收和实收独立，失败重试只计实际成功，回单不重记款 |
| 收款待核对 | P12 → P17 → P18 | 无流水有交接，一笔10000仅分配可用额 |
| 供应商待追回 | P10-B核验 → P07-C → P23 → P24 | 客户已退仍追供应商，8000退5000余3000 |
| 转款 | P09-D → P19 | 原款上限、双方公司与申请／成功／冲正各自可查 |
| 核算关账 | P25 → P26 | 不平不推，正常余额不清零，未解阻断不关账 |

P01、P02、P03、P16作为已完成成果保留回归，不重做。上表“核验”表示复查同一成果，不重复实施。若继续混合签证工作线，下一条建议复制 **P09-A**；也可直接复制其他独立指令。

## 按菜单快速定位与执行状态

此表记录本文件指令是否执行，不能将“指令已编写”改成“页面已验收”。

| 指令 | 当前菜单／页面任务 | 核心项 | 状态 |
|---|---|---|---|
| [P01](#p01) | 资源 → 供应商：公司协议来源保留核验 | 核01 | 已完成，仅保留核验 |
| [P02](#p02) | 产品中心 → 外采产品 → 产品市场：代理入库供给条件保留核验 | 核01、核06 | 已完成，仅保留核验 |
| [P03](#p03) | 产品中心 → 自营产品 → 产品管理：产品允许混合签证方式 | 核02 | 已完成，仅保留核验 |
| [P04-A](#p04-a) | 履约中心 → 团期管控 → 详情：订单名额归还原渠道 | 核03 | 已完成，仅保留核验 |
| [P04-B](#p04-b) | 履约中心 → 团期管控 → 详情：已售团核心变更不覆盖原依据 | 核04 | 待执行 |
| [P05](#p05) | 履约中心 → 交通采购：三类采购减量与付款计划 | 核05 | 待执行 |
| [P06](#p06) | 履约中心 → 签证办理：代办任务只包含委托游客 | 核02 | 待执行 |
| [P07-A](#p07-a) | 履约中心 → 团期结算：供应商费用对账与差异处理 | 核01保留、核09 | 待执行 |
| [P07-B](#p07-b) | 履约中心 → 团期结算：结算金额依据与二次结算 | 核10 | 待执行 |
| [P07-C](#p07-c) | 履约中心 → 团期结算：确认供应商应追回责任 | 核12 | 待执行 |
| [P08-A](#p08-a) | 销售中心 → 产品预订 → 填写：参考余位生成待供应确认订单 | 核06 | 待执行 |
| [P08-B](#p08-b) | 销售中心 → 产品预订 → 填写：保留订单原渠道占用依据 | 核03 | 已完成，仅保留核验 |
| [P08-C](#p08-c) | 销售中心 → 产品预订 → 填写：价格规则、营销减免与特批分开 | 核07 | 待执行 |
| [P09-A](#p09-a) | 销售中心 → 订单管理 → 详情：游客逐人选择签证方式 | 核02 | 待执行 |
| [P09-B](#p09-b) | 销售中心 → 订单管理 → 详情：供应回复及补差客户决定 | 核06 | 待执行 |
| [P09-C](#p09-c) | 销售中心 → 订单管理 → 详情：应收调整保留原价并只生效一次 | 核07、核10 | 待执行 |
| [P09-D](#p09-d) | 销售中心 → 订单管理 → 详情：转款申请核对原款和双方订单 | 核13 | 待执行 |
| [P10-A](#p10-a) | 销售中心 → 售后处理：退团转团的资源结果与无位承接 | 核03、核04、核06 | 核03已完成；核04、核06待执行 |
| [P10-B](#p10-b) | 销售中心 → 售后处理：客户退款与供应商待追回分开 | 核10、核12 | 待执行 |
| [P11](#p11) | 销售中心 → 合同管理：一单多份个人合同与覆盖核对 | 核08 | 待执行 |
| [P12](#p12) | 销售中心 → 收款认领：无流水凭证交财务核对与分配上限 | 核11 | 待执行 |
| [P13](#p13) | 供应商端 → 订单确认：按当前订单回传四种资源结果 | 核06 | 待执行 |
| [P14](#p14) | 供应商端 → 对账结算：逐单异议和重新确认保留历史 | 核09 | 待执行 |
| [P15](#p15) | 财务 → 成本应付 → 供应商账单：挂接不覆盖成本、差异按依据处理 | 核09 | 待执行 |
| [P16](#p16) | 财务 → 成本应付 → 付款申请：公司协议与历史责任保留核验 | 核01 | 已完成，仅保留核验 |
| [P17](#p17) | 财务 → 收款应收 → 到账流水：待核对申请与同笔真实到账关联 | 核11 | 待执行 |
| [P18](#p18) | 财务 → 收款应收 → 认款复核：到账证据和本次分配双重校验 | 核11 | 待执行 |
| [P19](#p19) | 财务 → 收款应收 → 转款处理：转款退回与冲正保留原记录 | 核13 | 待执行 |
| [P20](#p20) | 财务 → 收款应收 → 退款执行：退款事实独立、失败重试留历史 | 核10 | 待执行 |
| [P21](#p21) | 财务 → 成本应付 → 付款执行：实际付款与回单、重试分别记录 | 核10 | 待执行 |
| [P22](#p22) | 财务 → 成本应付 → 回单核销：核销不重复记付款 | 核10 | 复用核验，缺口才改 |
| [P23](#p23) | 财务 → 成本应付 → 预付管理：待退回与实际预付余额分开 | 核12 | 待执行 |
| [P24](#p24) | 财务 → 成本应付 → 付款退回：未到账及部分退回持续核对 | 核12 | 待执行 |
| [P25](#p25) | 财务 → NC推送：凭证校验统一阻断不平推送 | 核14 | 待执行 |
| [P26](#p26) | 财务 → 结算对账 → 期间关账：正常余额与真正阻断分别判断 | 核14 | 待执行 |

## 资源执行指令

<a id="p01"></a>
### P01｜资源 → 供应商｜公司协议来源保留核验

状态：已完成，仅保留核验。主页面：[supplier-detail.html](../merchant/resource/supplier-detail.html)。

```text
任务：P01 公司协议来源保留核验（主方案P01，核01）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。
本条为已完成成果的保留核验；默认不修改页面，只有发现既有承诺能力退化才作最小修复，不重新建设。

菜单：资源 → 供应商
主页面：merchant/resource/supplier-detail.html
当前代码定位（先核实最新版本）：shared/supplier-company-agreements.js；公司合作与协议
角色与交接：供应商经办维护各公司合作，产品、计调及财务引用协议。

本次具体执行：
1. 本指令只核验已完成成果，不重新建设。直接打开供应商详情，核对同一供应商的北京、上海合作公司及协议；供应商列表保持用户回撤后的原状，不补列表入口。
2. 核对新业务可引用范围：公司、供应商、生效状态、有效期及合作内容对应；多份有效协议保留选择，待批、退回、到期记录可查但不作为普通新业务候选。
3. 新合作待批不能替换另一有效协议；历史成本和付款继续显示原协议、起算事件、账期及责任，不能自动换成续签版。
4. 已满足则只记录本次核验结果。如发现这三项已有能力退化，只修退化点；不扩改供应商准入、审批流程、财务账户或列表。

必须通过的业务例子：
- 北京协议出票确认后30天、上海账单确认后45天分别可核对；不能串用。
- 北京新合作待批，上海有效协议仍可引用；到期原协议的历史引用可查看。
- 运行现有 scripts/check-supplier-company-agreements.cjs；说明是否实际进行了页面交互核验。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

## 产品中心执行指令

<a id="p02"></a>
### P02｜产品中心 → 外采产品 → 产品市场｜代理入库供给条件保留核验

状态：已完成，仅保留核验。主页面：[product-market.html](../merchant/product/product-market.html)。

```text
任务：P02 代理入库供给条件保留核验（主方案P02，核01、核06）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。
本条为已完成成果的保留核验；默认不修改页面，只有发现既有承诺能力退化才作最小修复，不重新建设。

菜单：产品中心 → 外采产品 → 产品市场
主页面：merchant/product/product-market.html
当前代码定位（先核实最新版本）：代理入库抽屉；shared/supplier-company-agreements.js
角色与交接：产品人员维护本公司代理条件，销售承接供给确认方式。

本次具体执行：
1. 只核验已完成的代理入库能力，保留原UI及保存草稿／提交入库按钮，不新增询位中心。
2. 换经营公司后旧协议清空，正式入库需对应公司有效协议；草稿可保存协议待补，但不能显示代理生效。
3. 参考余位20、置零询位、确认库存、沿用提供方方式分别展示正确销售条件。参考余位只表示参考，订单仍需供应确认；确认方式未知不得推成确认库存。
4. 草稿重开保留所选日期、数量、确认方式和公司；正式提交后详情保留这些销售使用条件。
5. 集团内部提供公司使用内部合作依据，不强迫选外部协议。保留核01，不改P08预订页和供应商端；它们由独立指令承接。

必须通过的业务例子：
- 参考20位明确需供应确认；置零仍可作为询位草稿，不冒称有可售保证。
- 北京切上海不沿用北京协议；无协议可保存草稿，不能正式提交。
- 内部产品引用内部依据通过；草稿重开及原确认型流程正常。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

<a id="p03"></a>
### P03｜产品中心 → 自营产品 → 产品管理｜产品允许混合签证方式

状态：已完成，仅保留核验。主页面：[product-self-edit.html](../merchant/product/product-self-edit.html)。实际修改和浏览器证据见[P03实施与验收](P03产品混合签证办理方式实施与验收-20260917.md)。除发现本条成果退化外，不重复执行下方指令；P09-A、P06仍各按自己的范围实施。

```text
任务：P03 产品允许混合签证方式（主方案P03，核02）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。

菜单：产品中心 → 自营产品 → 产品管理
主页面：merchant/product/product-self-edit.html
当前代码定位（先核实最新版本）：shared/product-visa-config.js：planHtml、lineVisaFormHtml、syncPolicyField；签证与证件
角色与交接：产品定义允许的办理方式；订单逐游客选择；签证专员只处理委托代办者。

本次具体执行：
1. 在原“签证与证件”区将办理方式互斥下拉改成可并选的“游客自办／随团办理”。先核对产品默认、线路方案与签证方案的实际控件，避免只改一个位置而另一个仍强制单选；保留现有方案层级，不新增一套重复配置。
2. “办理方式”只表达谁办理；包含团费、单独收费、客人自理等收费含义保留在原费用口径，不混作第三种办理方式。保留国家、签证类型、材料版本、办理期限及证件要求。
3. 随团办理的材料要求在选中该方式时可维护；同时勾自办不清空代办资料。自办游客仍有出行证件核对要求。
4. 需签证产品保存或提交前至少选一种允许方式；免签／不涉及继续沿原流程，不强制代办。非法选择就地提示，取消不改变原配置，保存后重开显示本次选择。
5. 共享脚本如被邮轮、自由行等编辑页引用，限定本次受支持页面，不能让改动破坏其他引用页。已有订单采用时的方式保留；后续产品修改不批量覆盖历史游客，不自动退费。
6. 本次不修改订单和签证工作台，只提供同产品的两方式样例给P09-A、P06承接。

必须通过的业务例子：
- 同时选自办和随团可保存；取消勾选至零不能提交需签证产品。
- 先填代办资料，再勾自办、保存、重开，原材料仍在。
- 免签例可保存；已采用产品的订单示例不随新配置改变。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

## 履约中心执行指令

<a id="p04-a"></a>
### P04-A｜履约中心 → 团期管控 → 详情｜订单名额归还原渠道

状态：已完成，仅保留核验。主页面：[schedules-detail.html](../merchant/tour/schedules-detail.html)。实际修改和浏览器证据见[P04-A实施与验收](P04-A订单名额归还原渠道实施与验收-20260918.md)。2026-09-18按用户后续要求，暂时隐藏尚未具备真实编辑能力的渠道价格与名额操作列；P08-B、P10-A仍各按自己的范围实施。

```text
任务：P04-A 订单名额归还原渠道（主方案P04，核03）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。

菜单：履约中心 → 团期管控 → 详情
主页面：merchant/tour/schedules-detail.html
当前代码定位（先核实最新版本）：价格名额；confirmReleaseSeat；countFromRow
角色与交接：团控处理原订单占用，售后提供退订依据；归还名额不等于退采购资源。

本次具体执行：
1. 在现有“释放占位”入口内补原占用明细选择及处理内容；若原小确认框无法承载逐笔选择，改用当前页处理抽屉，最后确认仍可复用小弹窗，不增加中间动作选择层。
2. 每笔原订单号、渠道、占用记录、数量单位、原占用、已释放、剩余占用只读；输入本次释放量与原因。多笔选择逐笔核对，不按整行总人数无条件释放。
3. 本次量必须为正整数且不超本笔剩余占用。提交时再次校验，更新该笔已释放、剩余占用及原渠道可用量；部分释放保持可继续处理，全部释放只保留查看。
4. 同次释放成功后再次点击不重复加名额；取消或校验失败不改数量。总可用量由明细结果核对，不沿用只给 scheduleData.available 累加人数的处理。
5. 原渠道停售仍归还原渠道并保持停售；不能改成直营或全渠道。需要重新分配走已有名额调整入口。
6. 原采购座位、舱房、铺位数量及已付款不变。保留邮轮不占舱儿童、第三人及专列不占铺儿童等已有规则，按实际占用单位释放。
7. 本次不做团期核心信息变更、不改销售售后页。

必须通过的业务例子：
- 代理配额10、占用6、可用4，释放2后占用4、可用6；直营数量不变。
- 重复释放同一处理结果不再增加；再释放5被拦截，零／负数被拦截。
- 代理停售后释放仍属代理；取消不占位游客不多释放整舱／整铺。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

<a id="p04-b"></a>
### P04-B｜履约中心 → 团期管控 → 详情｜已售团核心变更不覆盖原依据

状态：待执行。主页面：[schedules-detail.html](../merchant/tour/schedules-detail.html)。

```text
任务：P04-B 已售团核心变更不覆盖原依据（主方案P04，核04）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。

菜单：履约中心 → 团期管控 → 详情
主页面：merchant/tour/schedules-detail.html
当前代码定位（先核实最新版本）：saveScheduleEdit；scheduleRoutePlan change监听；顶部编辑
角色与交接：计调核对团期变更影响，销售通过既有售后转团承接订单与客户决定。

本次具体执行：
1. 保留原编辑抽屉。先判断是否有关联订单、成本或交通承诺；筹备且无上述依据的团期继续普通编辑。
2. 有关联业务时，出发／回团日期、产品方案等核心内容不能由普通保存覆盖。核心字段显示当前值和“办理变更”入口，普通不影响承诺的资料仍可维护。
3. 检查输入时的监听：当前 scheduleRoutePlan 的 change 直接改 scheduleData.route，必须改为只改编辑草稿；取消、关抽屉、校验失败都不能提前改原团期。
4. 在本页补“办理变更”处理区：原值只读、拟变值、原因／依据可填；列受影响订单、交通、合同、费用及未核实项，不虚构已全部同意。
5. 提供目标团期选择，显示日期、方案、名额及差价核对；没有合适目标时提供现有新建团期去向。保存形成变更申请／待处理记录，保留原团，后续走已有售后转团。
6. 不自动搬迁旅客、合同、收付款或采购；不得只改原团号表示完成。仅时刻变化且无费用是否可直改仍待确认，不能据此开放所有交通变更。
7. 本次只改核心变更区，保留P04-A的归还渠道成果。

必须通过的业务例子：
- 无订单无成本团期正常改日期；有订单和机票成本的团期原日期不能直接被改。
- 变更方案后取消，原方案不变；保存变更申请后原团和原单仍在。
- 有目标／无目标两种情况均有明确去向；目标名额不足不标迁移完成。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

<a id="p05"></a>
### P05｜履约中心 → 交通采购｜三类采购减量与付款计划

状态：待执行。主页面：[resource-procurement-inventory.html](../merchant/tour/resource-procurement-inventory.html)。

```text
任务：P05 三类采购减量与付款计划（主方案P05，核05）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。

菜单：履约中心 → 交通采购
主页面：merchant/tour/resource-procurement-inventory.html
当前代码定位（先核实最新版本）：saveChangeOrder；changeDrawer；kind=air/cruise/train
角色与交接：采购／计调按原合同提出变更；团控处理分配冲突，财务承接批准后的付款安排。

本次具体执行：
1. 同页覆盖机票切位、邮轮包舱、专列包铺三个菜单参数。保留合同详情、分配、付款节点及原“付款变更／发起变更”入口。
2. 抽屉按原合同→逐规格变更→分配影响→付款影响排列。每规格原数量、单价、已分配、其中已售只读，变更后数量可填，原因／依据必填；多舱型或铺型必须逐项输入，不能统一乘 primaryUnitPrice。
3. 已售属于已分配的组成，不将两者相加。逐规格核对变更后数量、有效分配、已售和剩余；不足时显示缺口及团期。允许保存待处理申请，不允许直接生效或改当前合同。
4. 修正 recalculated || originalAmount：有效零值必须保留0，缺金额与0分开。按各规格金额及合同节点核算未付计划，不无依据推定免损或退款；已有付款金额、日期、原节点记录只读保留。
5. 待抵扣、待退回、需补付分别有依据和金额；减量不能即记供应商已退款。批准前原有效数量、付款计划不变，提供明确已批准的本页样例演示生效后新版本。
6. 变更记录保留前后数量、金额、分配冲突、原因、批准依据和生效结果；重复确认不再次减量或重复抵扣。保留原正常新增、分配与付款申请入口，不实现自动请款／自动审批付款。
7. 超分配损失、押金、免损期限按已有合同或待确认规则处理，不在代码里制定新政策。

必须通过的业务例子：
- 100座分配60（其中已售40），改80剩余可分配20；改50缺10，不能直接生效。
- 无占用、未付款且明确批准全退免损：10×1000减为0，未付计划为0，不能回弹10000。
- A舱10×2000、B舱5×3000改8和4，新资源金额28000。
- 已付记录在变更前后相同；三个kind入口各验证正常与冲突。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

<a id="p06"></a>
### P06｜履约中心 → 签证办理｜代办任务只包含委托游客

状态：待执行。主页面：[visa-processing.html](../merchant/tour/visa-processing.html)。

```text
任务：P06 代办任务只包含委托游客（主方案P06，核02）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。

菜单：履约中心 → 签证办理
主页面：merchant/tour/visa-processing.html
当前代码定位（先核实最新版本）：openDrawer、filterRows及data-save-visa-process
角色与交接：签证专员接收订单逐人委托，不负责对客改价或退款。

本次具体执行：
1. 保留原办理列表、筛选和办理抽屉。使用P03与P09-A同一三人订单样例；代办列表只建两位委托随团办理者，自办者不能伪装成待送签任务。
2. 办理详情带出订单、游客、办理方式、对应产品方案及委托依据；识别以游客记录区分，不能只按同行姓名猜或重复建客户。
3. 待送签、已送签和出签人数依据实际代办任务统计；同行自办者仅在关联订单查看，材料缺口不将其计为代办未完成。
4. 方式变更时，尚未办理者按已批准变更结束原代办任务并留原任务、原因和时间；已送签／有费用者保持原办理记录，显示待签证经办核实结果，不因选自办而删除。
5. 保存本次处理后重开同人仍可查看结果，不能改到另一行；费用变化仅提供订单应收调整承接，不在签证页直接退钱。
6. 若P03/P09-A尚未执行，先用明确独立样例完成本页，不顺带修改它们或建设同步服务。

必须通过的业务例子：
- 3人中1人自办、2人随团，仅2条代办任务，待办人数不含自办者。
- 未送签改自办保留结束记录；已送签改方式不能抹掉原任务或费用。
- 连开两个游客处理结果互不串行；原送签、出签流程仍能操作。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

<a id="p07-a"></a>
### P07-A｜履约中心 → 团期结算｜供应商费用对账与差异处理

状态：待执行。主页面：[fulfillment-cost.html](../merchant/tour/fulfillment-cost.html)。

```text
任务：P07-A 供应商费用对账与差异处理（主方案P07，核01保留、核09）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。

菜单：履约中心 → 团期结算
主页面：merchant/tour/fulfillment-cost.html
当前代码定位（先核实最新版本）：成本明细及成本详情；公司协议与付款责任依据
角色与交接：计调核对本团费用并提出业务处理意见，供应商确认其账单，财务采用批准依据。

本次具体执行：
1. 只处理成本详情的对账区。保留核01采购公司、实际供应商、公司协议及历史到期依据；产品内部提供公司不覆盖各项实际采购供应商。
2. 先复用现有供应商核对入口；确实缺少时，在成本详情补“发起对账”，不新增菜单，不改供应商列表的发起设置。
3. 同公司、供应商、币种选择本次成本行，带出费用、原确认金额、协议及成本依据；跨公司或不同币种不能混成一张普通核对单。
4. 发起后本页留本次对账号、明细范围、我方金额、发起时间和待供应商确认结果，不能同时显示双方已确认。材料的发起方向有冲突，暂按主方案我方发起的独立样例表达，不将其写为集团制度。
5. 供应商82000与我方80000时显示2000差异及逐项原因。计调从原成本处理变更，原80000保留；引用批准增加1000后我方当前81000，剩差1000，不能因挂接、发送或对方回复自动清零。
6. 已提交及已确认范围只读；重提或重新确认保留前版、异议和新处理依据，结果交P14供应商确认及P15财务核对。
7. 本次不改收入收款、二次结算及追款区，它们分别由P07-B/C执行。

必须通过的业务例子：
- 选本次供应商成本，发送只形成待确认；另一供应商费用不串入。
- 80000／82000差2000；批准增加1000后差1000，前次依据仍可查。
- 公司协议仍可核对；待批成本调整不改变已确认成本。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

<a id="p07-b"></a>
### P07-B｜履约中心 → 团期结算｜结算金额依据与二次结算

状态：待执行。主页面：[fulfillment-cost.html](../merchant/tour/fulfillment-cost.html)。

```text
任务：P07-B 结算金额依据与二次结算（主方案P07，核10）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。

菜单：履约中心 → 团期结算
主页面：merchant/tour/fulfillment-cost.html
当前代码定位（先核实最新版本）：settlementTotals、settlementIncomeRows；二次结算
角色与交接：计调按逐订单业务和已确认成本提交结算，财务另行确认会计收入及期间。

本次具体执行：
1. 本次只修改结算详情的金额来源与二次结算，保留成本维护、公司协议、对账和付款申请。复用既有TG-01，不另开重复任务。
2. 用明确逐订单成交／有效调整、逐笔实际收退、逐项已确认成本的独立样例代替62%/38%反推明细；不要求从其他页面真实取数。每行有订单或原收款／退款／成本及调整依据，不以拼接团号造出金额来源。
3. 订单区显示原成交、生效调整、当前应收；收退区显示累计收款、已退款、净收及待退；成本区只汇总当前已确认成本。当前应收=原成交+已生效调整，净收=累计成功收款-累计成功退款，两者独立。
4. 已含优惠的成交额不再扣同一优惠；已生效减价不得在退款成功时再冲一次应收。0保留0，未知金额显示待补并阻止把不完整数称完整结算，不用团期收入兜底成实收。
5. 结算与会计收入区分；不凭净收算会计收入。保留现有业务毛利，只在同一明确业务金额与确认成本基础上计算，缺依据不造毛利。
6. 现有二次结算展示原确认、本次差额、调整后结果，原记录只读。本次保存草稿／提交审批不覆盖原确认结果；批准的独立样例只应用一次差额。
7. 原付款申请继续带协议及责任，不改银行执行，不新增会计确认页面。

必须通过的业务例子：
- 成交100000、批准减价5000、累计收款100000：应收95000，退款前待退5000；实退5000后累计已收仍100000、净收95000，应收仍95000。
- 未批准减价不减少应收；实收明确0仍为0；缺收款金额不得用100000兜底。
- 原成本70000、后补批准2000：原记录70000、本次2000、调整后72000；重复查看／提交不再增加。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

<a id="p07-c"></a>
### P07-C｜履约中心 → 团期结算｜确认供应商应追回责任

状态：待执行。主页面：[fulfillment-cost.html](../merchant/tour/fulfillment-cost.html)。

```text
任务：P07-C 确认供应商应追回责任（主方案P07，核12）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。

菜单：履约中心 → 团期结算
主页面：merchant/tour/fulfillment-cost.html
当前代码定位（先核实最新版本）：成本详情；原成本、付款及预付关联区
角色与交接：计调核实售后对应供应商承担，财务在预付管理／付款退回办理实际回款。

本次具体执行：
1. 只补当前成本详情“供应商退款／抵扣”处理；已有同义入口则复用，不在一级列表另增追款按钮。
2. 带出关联售后、原成本、成本公司、供应商、币种、原付款或预付及剩余未处理范围；使用选择现有记录，不能以整团主供应商代替实际承担者。
3. 录入拟追回额、责任原因及依据；未获确认时保存待责任确认，不直接当作财务应收或负应付。已有供应商确认／批准依据时记录确认金额、确认人和时间，保留前次意见。
4. 在详情并列展示客户批准退款与供应商确认应退，分别显示已退和未结。客户退款完成不改变供应商应追余额；供应商承担不能简单等于客户退额或全部预付。
5. 确认后按原款性质提供现有预付管理或付款退回的关联办理入口及业务编号；暂不能确定正式单据类型时保留待财务核对，不伪造已生成会计单据。
6. 退款与抵未来费用分开；仅供应商同意未来抵扣尚未实际抵扣，仍保留未结。不实现跨页自动回写。

必须通过的业务例子：
- 客户退10000，供应商确认承担8000：分别显示，不能同额套用。
- 供应商已退5000样例仍有3000未追回；客户退款已完成仍能看见。
- 未确认责任不形成已确认追款；同原成本重复提交不能重复计同一责任。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

## 销售中心执行指令

<a id="p08-a"></a>
### P08-A｜销售中心 → 产品预订 → 填写｜参考余位生成待供应确认订单

状态：待执行。主页面：[booking.html](../merchant/sales/booking.html)。

```text
任务：P08-A 参考余位生成待供应确认订单（主方案P08，核06）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。

菜单：销售中心 → 产品预订 → 填写
主页面：merchant/sales/booking.html
当前代码定位（先核实最新版本）：mode为pending的处理、订单生成及createdText；P02产品市场结果只读参考
角色与交接：销售提交预订请求，供应商回复后由计调和销售分别核资源与客户决定。

本次具体执行：
1. 延续P02已完成成果，使用同一参考余位20产品样例；无需读取产品市场本地数据。保留原预订步骤、游客填写、草稿及正常确认型产品流程。
2. 在既有订单确认区展示参考余位及“需供应确认”，与已有确认库存区分；未取得确认号／有效回复时，即使用户选择确认也不能生成资源已确认的订单。
3. 复用已有“待确认”主状态，详情或结果中解释为待供应确认；记录请求产品、团期、人数／规格、提供方、确认方式及本次请求。未实际占用时保留未占位，不能因参考数20制造20个保证库存。
4. 提交成功展示本次订单号、待供应确认、负责计调及订单详情入口，不同时显示确认生效或自动生成实际收款／合同签完。草稿与已提交请求分开。
5. 有明确有效确认库存的正常样例继续原确认流程；暂保截止只用有效回复，不写死24／48小时。待确认能否收款／签约依据原批准条件，未定保持待核实。
6. 本次只处理供给条件与提交结果，不改价格优惠或渠道配额；保留其他已完成规则。P13、P09-B、P10-A由后续指令承接。

必须通过的业务例子：
- 同一参考20产品提交后有订单号、待供应确认、未冒称已保证库存。
- 切换为确认模式不能绕过供应条件；保存草稿不占位。
- 正常确认库存例仍可按原条件确认；无批准付款／签约政策不新设门槛。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

<a id="p08-b"></a>
### P08-B｜销售中心 → 产品预订 → 填写｜保留订单原渠道占用依据

状态：已完成，仅保留核验。主页面：[booking.html](../merchant/sales/booking.html)。88-07已完成登录归属锁定、渠道配额核对、占用记录及数量单位保存，待确认／草稿不伪造占位；见[实施与验收](88-07渠道名额占位原渠道释放实施与验收-20260918.md)。

```text
任务：P08-B 保留订单原渠道占用依据（主方案P08，核03）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。

菜单：销售中心 → 产品预订 → 填写
主页面：merchant/sales/booking.html
当前代码定位（先核实最新版本）：渠道选择、占位确认及订单生成记录
角色与交接：销售按实际来源渠道提交，团控和售后以后据原占用归还。

本次具体执行：
1. 只处理当前订单的销售渠道和名额占用依据，保留P08-A供给状态及现有价格逻辑。
2. 在原占位确认区显示实际销售渠道、所选团期、对应配额、本次人数与实际占用单位；取明确渠道资料，不让经办用隐藏默认值将代理订单扣直营名额。
3. 在本页订单结果中保留发生时渠道、对应占用记录及数量；待供应确认未占位者不伪造已扣配额。草稿不产生正式占用。
4. 换渠道或团期后清除前次配额核对结果，重新检查是否有对应可用量。缺渠道或渠道额度不足时不能静默改扣全渠道；走现有渠道调整去向。
5. 保存后原占用与销售归属分别可核对，后续售后释放用原占用渠道，不用经办当前门店或当前渠道替代。
6. 只在本页内演示数量及结果，不真实修改团期页数据，也不加新的渠道库存配置菜单。

必须通过的业务例子：
- 代理可用4，下单占2后本页结果显示代理占用2，不动直营示例。
- 代理可用1时申请2不得偷用直营额度；改渠道重新核对。
- 未占位／草稿无实际扣减；P04-A和P10-A可使用该原占用样例。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

<a id="p08-c"></a>
### P08-C｜销售中心 → 产品预订 → 填写｜价格规则、营销减免与特批分开

状态：待执行。主页面：[booking.html](../merchant/sales/booking.html)。

```text
任务：P08-C 价格规则、营销减免与特批分开（主方案P08，核07）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。

菜单：销售中心 → 产品预订 → 填写
主页面：merchant/sales/booking.html
当前代码定位（先核实最新版本）：currentDiscountUsages、calcTotal、customerPayableAmount；原价格区
角色与交接：销售确认成交依据，特殊折扣由现有审批承接，供应成本与渠道佣金独立。

本次具体执行：
1. 在原价格区按团期批准售价→适用价格条件→独立营销减免→客户应付列本单依据。使用现有定价策略和营销资料的明确样例，不建全量规则引擎或新配置页。
2. currentDiscountUsages不能只因订单总额或渠道名称命中就自动给固定优惠；给当前可选规则及适用条件，核对团期、报名日期、人数和资格，不适用项不可选且有就近原因。
3. 已在售价中包含的早鸟／阶梯价保留采用依据，但不在营销区再减一次。营销优惠逐项保留券／活动号、金额和承担方金额；各承担方之和等于本项优惠，同项不重复应用。
4. 金额或人数、团期、渠道、日期改变时重新核价，不保留已失效优惠。叠加优先级没有批准依据时保留待核价／草稿，不能自动全叠加。
5. 原特殊折扣入口能用则复用；否则在价格区补申请抽屉，显示原批准价，只填申请价、原因、范围与依据，交现有审批。申请未批不作为正式批准成交价；本页可用已批准例展示结果，但不得伪造审批中心已审批。
6. 渠道佣金不再从客户应付扣一次；供应商补差不直接当对客加价。本次不修改成本、佣金配置、营销规则管理或生产权限。

必须通过的业务例子：
- 基础10000→符合早鸟后9500→独立营销减300→客户应付9200，不能变8700；营销300可用已给定210+90承担例核对。
- 相同总价但不满足早鸟日期／资格，不能套同规则；修改人数使资格失效后重新核算。
- 特批待审保留原有效价，退回不改成交；同优惠重复点击不重扣。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

<a id="p09-a"></a>
### P09-A｜销售中心 → 订单管理 → 详情｜游客逐人选择签证方式

状态：待执行。主页面：[orders-detail.html](../merchant/sales/orders-detail.html)。

```text
任务：P09-A 游客逐人选择签证方式（主方案P09，核02）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。

菜单：销售中心 → 订单管理 → 详情
主页面：merchant/sales/orders-detail.html
当前代码定位（先核实最新版本）：travelerEditCard；shared/order-visa-detail.js的统一旅行社代办及names.map
角色与交接：销售核对每位游客选择；签证专员接收实际代办任务。

本次具体执行：
1. 在原游客编辑中给每人添加“签证办理方式”下拉，只列该单采用的产品允许方式，不能一个整单选项覆盖所有游客。
2. 用游客记录身份保存选择，不只按姓名匹配；同订单3人分别保留1人自办、2人随团，原证件资料及材料入口不变。
3. 修改 shared/order-visa-detail.js 当前统一旅行社代办及全名单生成方式：按逐人选择显示，自办不列成未送签，代办汇总仅数2人；不涉及签证的产品保持原规则。
4. 变更前检查此游客原代办任务阶段。尚未办理可按批准变更留记录；已送签或发生费用者不能仅保存新选项就删除任务，显示待经办处理并保留旧有效结果及变更原因。
5. 产品后续减少允许方式不追改历史游客。自办与退费分开，有价格影响走现有应收调整，不自动减钱。
6. 同页保存、取消、重开能核对结果；本次不改其他订单处理区，不重建客户或签证工作台。

必须通过的业务例子：
- 3人混合选择重开仍逐人保留，代办2人；产品只允许随团时不能选自办。
- 已送签改自办不能擦掉任务；取消编辑不改变方式。
- 自办不自动降低订单应收，原证件核对功能正常。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

<a id="p09-b"></a>
### P09-B｜销售中心 → 订单管理 → 详情｜供应回复及补差客户决定

状态：待执行。主页面：[orders-detail.html](../merchant/sales/orders-detail.html)。

```text
任务：P09-B 供应回复及补差客户决定（主方案P09，核06）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。

菜单：销售中心 → 订单管理 → 详情
主页面：merchant/sales/orders-detail.html
当前代码定位（先核实最新版本）：原供应确认／业务处理区；订单主操作、资源明细
角色与交接：计调核资源及供应报价，销售记录客户决定；没有销售顾问时由本单计调承接。

本次具体执行：
1. 使用P08-A、P13同一组独立样例，在原资源／业务处理区域补本订单请求与供应回复，不新建询位菜单。
2. 四种回复分别显示：确认的确认号与范围；暂保的范围与截止；补差的供应报价、币种、原因及有效条件；无位的范围与原因。多次回复保留前次记录及采用情况，不能只覆盖最新文字。
3. 供应商确认资源后仍检查该单其他既有确认条件，不由一条回复直接把所有订单标已完成。部分范围未确认保留未确认部分，不扩张成完整确认。
4. 补差先进入当前页的计调核价处理，核对供应成本变化与拟对客方案；销售记录客户接受／拒绝及依据。客户未接受前原成交价、应收不变；涉及改价复用P09-C应收调整。
5. 无位或客户拒绝提供现有售后改期／取消入口及来源回复编号；已有收款不在本页直接完成退款。暂保过期保留过期事实并重新确认，不能当长期确认。
6. 同一订单无销售时显示实际计调责任，不能让处理无人接；只使用页面样例，不跨端真实同步或自动通知客户。
7. 状态与动作按订单主状态表达，供应回复作为依据，不在列表另堆多套按钮。

必须通过的业务例子：
- 补差1000只改变待核价依据，客户未接受时原应收不变；接受后仍走批准调整。
- 无位可进入已有售后；暂保截止已过不能直接确认。
- 两笔订单各自回复不串；前次暂保和后次确认均可查。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

<a id="p09-c"></a>
### P09-C｜销售中心 → 订单管理 → 详情｜应收调整保留原价并只生效一次

状态：待执行。主页面：[orders-detail.html](../merchant/sales/orders-detail.html)。

```text
任务：P09-C 应收调整保留原价并只生效一次（主方案P09，核07、核10）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。

菜单：销售中心 → 订单管理 → 详情
主页面：merchant/sales/orders-detail.html
当前代码定位（先核实最新版本）：receivableDrawer；提交应收调整处理及金额详情
角色与交接：销售提交价格变化，审批人批准，财务另办实际收退。

本次具体执行：
1. 保留已有应收调整抽屉和审批入口，原成交及已生效调整只读；本次输入增减方向、调整金额、原因、所涉游客／费用、价格规则或审批依据。
2. 申请、审批、生效结果分开；提交后进入审批中，当前有效应收不变。退回可补充重提并保留原申请额和意见，不先扣钱再回滚。
3. 已批准样例保留审批号、批准范围、批准时间、原应收、本次变化和调整后应收；同一批准调整重复确认不能重复应用，不用整段订单金额覆盖历史。
4. 供应补差需计调核价及客户决定；营销减免或早鸟已包含在成交价中时不可重复调整。特殊折扣待批不能直接当已批准价格。
5. 实际收款、已退款、待退款分开显示。应收降低只形成金额责任，不代表出纳已退款；实际退款成功也不能再次降低应收。
6. 已结算业务新增调整沿原二次结算／结算调整承接，保留原结算结果。本次不改财务执行或订单游客、转款区。

必须通过的业务例子：
- 原100000，减5000待审仍100000；批准后95000；退回不改原金额。
- 重复采用该批准记录仍95000；退款5000成功后应收仍95000。
- 收款100000而应收95000可见待退5000，不能显示应收负数或已完成退款。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

<a id="p09-d"></a>
### P09-D｜销售中心 → 订单管理 → 详情｜转款申请核对原款和双方订单

状态：待执行。主页面：[orders-detail.html](../merchant/sales/orders-detail.html)。

```text
任务：P09-D 转款申请核对原款和双方订单（主方案P09，核13）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。

菜单：销售中心 → 订单管理 → 详情
主页面：merchant/sales/orders-detail.html
当前代码定位（先核实最新版本）：afterSalesFieldsHtml转款分支；原售后申请抽屉
角色与交接：销售选择款项及目标订单，财务核对责任并确认分配，转款不新增银行到账。

本次具体执行：
1. 复用现有售后转款申请，目标订单由自由文本改成可搜索选择的现有订单。抽屉按原订单／原收款→目标订单→双方核对→金额→原因依据排列。
2. 原款选择显示流水、收款公司、客户、金额、已退、已转及有效在途占用；目标显示订单号、收款公司、客户、门店、状态、当前待收。选目标后重新计算，不保留前目标核对结论。
3. 可转额按原款减已退、有效已转及有效占用，采用明确未重叠的记录算，不能重复扣同一笔。新申请额正数且不超原款可用及目标本次可承接范围；需要超收处理的走现有批准去向，不自动抹零目标待收。
4. 已取消目标、原款不足、公司／币种不符不能普通提交成功。同公司跨客户／销售／门店按已有适用审批承接；无批准规则时保留待核实，不擅自实现自动审批。
5. 跨收款公司不按普通转款放行；保留差异与财务核对去向，不将附件的先退再收当已批准通用制度。
6. 提交形成转款申请，保留申请金额、原款、目标及原因，不新建银行流水，不直接标已核销；原记录及退回可在关联区域查看。
7. 本次仅申请页，财务确认和冲正由P19处理。

必须通过的业务例子：
- 原收10000、已退2000、已转3000且无占用，可转5000；申请6000拦截。
- 目标待收6000申请4000，结果为待处理申请，不直接显示待收0。
- 换目标后公司及额度重新核对；取消订单／跨公司普通转款不可直接成功。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

<a id="p10-a"></a>
### P10-A｜销售中心 → 售后处理｜退团转团的资源结果与无位承接

状态：部分完成。主页面：[orders-after-sales.html](../merchant/sales/orders-after-sales.html)。88-07已完成核03所需的原占用渠道、实际释放、原渠道余额及资源／款项结果分区，见[实施与验收](88-07渠道名额占位原渠道释放实施与验收-20260918.md)；目标无位、供应回复承接及整团核心变更仍属核04、核06待执行范围。

```text
任务：P10-A 退团转团的资源结果与无位承接（主方案P10，核03、核04、核06）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。

菜单：销售中心 → 售后处理
主页面：merchant/sales/orders-after-sales.html
当前代码定位（先核实最新版本）：现有详情资料、renderDetail、退团／转团及供应无位样例
角色与交接：销售管理售后，计调确认资源变化；不把取消申请或退款成功当作资源处理完成。

本次具体执行：
1. 保留现有售后列表及按主状态进入详情的操作，不新增重复售后模块。现有主发起入口在订单详情时继续使用；本页补当前售后必要处理区，不再建一套申请。
2. 退团／取消详情展示原订单、游客范围、原占用渠道与记录、本次拟释放量、实际释放量、计调确认依据。确认释放只按批准数量归原渠道，不自动归直营，也不自动退掉采购资源。
3. 转团保留原团号、日期及占用，选择目标团期，核对目标名额、日期、方案、价差及合同影响；目标确认与原占用释放分别有结果，不能保存后只把原团号换掉。
4. 目标无位／待确认时，保留原资源情况和未完成步骤，不报转团完成。沿已有不同业务的舱房、铺位及不占位规则，不将游客数当所有资源单位。
5. 供应商无位订单引用原回复，提供现有改期或取消分支；已有收款进入退款依据核对，未收款显示无需实际退款，不能生成一笔假退款。
6. 关联合同、费用、退款进入原办理入口；本次不实现全团迁移或自动改合同／款项。保留P10-B财务责任内容。

必须通过的业务例子：
- 代理取消2位，实际释放归代理；退款成功但尚未释放时仍显示未处理资源。
- 转团原团、目标团都可查，目标名额不足不完成；不占舱儿童取消不释放整舱。
- 无位且未收款有取消结果但实际退款0；有收款则显示待退款承接。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

<a id="p10-b"></a>
### P10-B｜销售中心 → 售后处理｜客户退款与供应商待追回分开

状态：待执行。主页面：[orders-after-sales.html](../merchant/sales/orders-after-sales.html)。

```text
任务：P10-B 客户退款与供应商待追回分开（主方案P10，核10、核12）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。

菜单：销售中心 → 售后处理
主页面：merchant/sales/orders-after-sales.html
当前代码定位（先核实最新版本）：售后详情中的退改规则、退款金额、供应商责任与关联记录
角色与交接：销售核对客户退改，计调核实成本承担，出纳退客款、应付财务追供应商款。

本次具体执行：
1. 在原售后详情／处理区分开本次应收减少、客户扣损、批准应退款、实际已退款；带出原收款、已退和本次可退，不按订单标价直接退款。
2. 客户退款引用已批准退改及优惠处理依据；政策未定的券退回／原价退款不写死。待批变化不改变有效应收，资金退款完成也不再扣一次成交。
3. 补供应商责任区：成本供应商、公司、币种、原成本、原付款或预付、本次拟承担、确认应退、依据、已回收及未结；多供应商逐项核对，不以主供应商概括全部。
4. 客户应退10000与供应商应退8000分别记录；客户完成退款仍保留供应商待退，不共用一个“已退款”结果。
5. 客户退款关联P20；供应商已确认责任关联P07-C，再由原款性质交P23/P24。没有责任确认仅留待核实，不自动生成负应付，不把全部预付视为可追回。
6. 客户先退及垫付范围仍需已有批准政策；可采用明确获批样例，但不因此全局放开先退。供应商同意未来抵扣不当银行到账。
7. 当前列表仍由售后主状态决定操作，不因待追款、待释放等辅助状态每行换一种按钮。

必须通过的业务例子：
- 客户批准退10000、已退10000；供应商确认8000、实退5000、待追3000，两条结果独立。
- 未确认供应商责任时不显示已追回；客款未付不显示实际退款成功。
- 应收减5000后资金退5000，不再次减应收；优惠未定例留待核实。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

<a id="p11"></a>
### P11｜销售中心 → 合同管理｜一单多份个人合同与覆盖核对

状态：待执行。主页面：[contracts.html](../merchant/sales/contracts.html)。

```text
任务：P11 一单多份个人合同与覆盖核对（主方案P11，核08）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。

菜单：销售中心 → 合同管理
主页面：merchant/sales/contracts.html
当前代码定位（先核实最新版本）：fillConfirm、fillDetail、confirmSubmitBtn；生成并提交审核
角色与交接：销售生成合同，审核人审核，游客分别签署；合同份数不改变订单笔数或成交额。

本次具体执行：
1. 保留原生成、审核、发送、线下归档流程；在生成区增加多人一份／分别生成及本次游客勾选，签约方式按明确适用样例显示，不要求接电子签约接口。
2. 分别生成时展示每份游客、签约人、费用范围或给定分配金额及预览。使用订单游客资料，不重复建订单，不让三份都复制整单30000。
3. 每位游客和费用范围有明确覆盖；同版本内重复覆盖、空选择、金额合计超所选范围均拦截。缺分配依据时保存待补，不自动按人数平均。
4. 生成并提交后每份有独立合同号、版本、覆盖人、金额和审核状态，仍关联同一订单；重复提交同一生成结果不能多出一套合同。
5. 详情能查看同订单各合同，完成状态按本次有效覆盖范围核对。两份签完、一份待签不能显示全部签完。
6. 已签合同变更沿现有补充／重新生成，原版保留并指明替代关系；新旧版本不重复计算销售金额。
7. 沿现有批准签约付款条件，不一律先付全款；模板与法律适用未定仅保留待补，不替法务批准新合同。

必须通过的业务例子：
- 一单3人30000，明确给定各10000，生成3份各10000，订单仍一笔30000。
- 2份签完1份待签，不显示全签；一份费用未知不自动均分。
- 重复游客／费用超分拦截；变更新版保留原已签版及替代关系。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

<a id="p12"></a>
### P12｜销售中心 → 收款认领｜无流水凭证交财务核对与分配上限

状态：待执行。主页面：[payment-claim.html](../merchant/sales/payment-claim.html)。

```text
任务：P12 无流水凭证交财务核对与分配上限（主方案P12，核11）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。

菜单：销售中心 → 收款认领
主页面：merchant/sales/payment-claim.html
当前代码定位（先核实最新版本）：data-flow-voucher-input change、data-submit-manual-claim、updateClaimSummary
角色与交接：销售说明客户付款归属，财务核实真实到账及可认金额。

本次具体执行：
1. 复用已有人工认领抽屉、查流水、多订单分配和附件。查到流水沿原流程；查不到时在同抽屉切“提交付款凭证”分支，底部主按钮为“提交核对”。
2. 待核对资料包含收款公司／账户、付款方、金额、付款日期、交易号或付款凭证、拟分配订单及代付关系。无交易号可留待财务核实，但不能编银行流水号；至少有可核对付款依据。
3. 删除任意上传文件就选中CMBC20260625001的固定匹配。上传只记录本文件及待核对资料；已知流水由经办选择确认，不宣称已完成真实识别。
4. 已有流水分配显示可用额、每单本次金额、合计、剩余。负数、全零、超可用总额和超可核销订单额度不能提交；超收引用现有预收／待认处理，不让订单待收负数。
5. 暂无流水的申请按所填付款金额核对拟分配，但所有金额只是申请，不增加订单实收、不核销。保存本页可重开的申请与状态，不能只关闭抽屉吐司成功。
6. 财务退回后在原申请补资料重提，保留原号、原金额、退回原因及历史。已经确认部分只读，未确认部分继续核对；同笔款不得重复新建收款。
7. 财务到账及复核分别由P17/P18完成，本页不造银行记录或跨页自动认款。

必须通过的业务例子：
- 10000分6000+2000，余2000待认；6000+5000拦截；负数或全0拦截。
- 上传任意文件不自动命中固定流水；无流水10000可提交核对，实收不变。
- 退回原申请补资料再提交，原申请额及退回原因仍可查。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

## 供应商端执行指令

<a id="p13"></a>
### P13｜供应商端 → 订单确认｜按当前订单回传四种资源结果

状态：待执行。主页面：[orders.html](../supplier/orders.html)。

```text
任务：P13 按当前订单回传四种资源结果（主方案P13，核06）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。

菜单：供应商端 → 订单确认
主页面：supplier/orders.html
当前代码定位（先核实最新版本）：data-open-order-drawer、openDrawer；回传确认结果当前仅关闭
角色与交接：供应商回复资源结果，我方计调核价核资源，销售取得客户决定。

本次具体执行：
1. 保留订单列表及原确认抽屉，将打开处理改为绑定所点击订单，显示该单产品、出发日期、人数／规格、公司和请求编号，不再共用固定订单。
2. 在同一处理抽屉选择确认／暂保／补差／无位，联动必填项：确认需确认号、范围及依据；暂保需范围和有效截止；补差需金额、币种、原因和适用条件；无位需无位范围与原因。
3. 只回传本次请求范围，缺确认依据或超范围不能标全部确认。暂保截止须有效，不用固定天数；补差为供应报价变化，不能直接修改我方客户价格。
4. 点击“回传确认结果”后保存本单回复、时间、处理人和各分支结果，详情重开可查；未完成／失败不改变原结果。再次回复留前次历史，不能静默覆盖确认依据。
5. 确认交我方继续、暂保待后续确认、补差待核价及客户决定、无位待改期取消，各有对应处理含义；不把四种结果都置已确认。
6. 原型只本页内保存，不跨端自动回传；P09-B用相同样例承接，不在供应商端批准对客改价、退款或发通知。

必须通过的业务例子：
- 两行不同订单打开分别显示当前请求，回复第二单不改第一单。
- 确认缺号、暂保无截止、补差无币种／金额、无位无原因均不能完成对应回复。
- 补差1000仍是供应报价回复，客户原应收不变；重开保留前次回复。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

<a id="p14"></a>
### P14｜供应商端 → 对账结算｜逐单异议和重新确认保留历史

状态：待执行。主页面：[settlements.html](../supplier/settlements.html)。

```text
任务：P14 逐单异议和重新确认保留历史（主方案P14，核09）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。

菜单：供应商端 → 对账结算
主页面：supplier/settlements.html
当前代码定位（先核实最新版本）：data-open-settlement-drawer、openDrawer；提出差异／处理差异
角色与交接：供应商核对我方成本范围并提出异议，计调处理差异，财务承接认可金额。

本次具体执行：
1. 将现有确认账单、提出差异、处理差异、查看明细绑定当前账单；按动作打开原抽屉相应处理区，不显示同一固定账单。
2. 摘要及明细显示本次公司、供应商、币种、协议、费用范围、我方金额、供应商金额及差额。原金额只读；不混不同公司或币种。
3. 提出差异须选具体明细，填本方金额、原因及依据；提交状态为有差异，双方原数保留，不自动清0，不自动完成付款。
4. “处理差异”在供应商端只核对我方回复及本方再确认，不能替我方批准成本。展示原异议、我方采用／拒绝原因、批准调整和剩余差额；用P07-A同例说明。
5. 我方调整后形成新确认版本，供应商对新范围重新确认；前版、前次异议和采用金额可查。残留未处理差额不能按确认按钮消失。
6. 复用已有发票提交等入口，对账确认不等于付款批准；盖章和电子依据按现有批准条件，未定不擅自取消。
7. 发起方向按主方案暂用我方发起样例，不改供应商列表设置、不新增另一套对账菜单。

必须通过的业务例子：
- 我方80000、对方82000差2000；批准加1000后我方81000、余差1000。
- 仍有1000未处理时不能显示双方一致；最终采用依据及前版均保留。
- 打开第二张账单处理不影响第一张；确认不新增实际付款。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

## 财务执行指令

<a id="p15"></a>
### P15｜财务 → 成本应付 → 供应商账单｜挂接不覆盖成本、差异按依据处理

状态：待执行。主页面：[finance-supplier-bills.html](../merchant/finance/finance-supplier-bills.html)。

```text
任务：P15 挂接不覆盖成本、差异按依据处理（主方案P15，核09）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。

菜单：财务 → 成本应付 → 供应商账单
主页面：merchant/finance/finance-supplier-bills.html
当前代码定位（先核实最新版本）：confirmWork的确认挂接／核对分支；systemOriginal、systemCost、diffOriginal、diffLocal
角色与交接：应付会计关联账单和业务成本，计调批准业务调整，财务核对后承接应付。

本次具体执行：
1. 保留识别、挂接、核对、差异和详情入口。在挂接抽屉将原账单公司、供应商、币种、期间、原币／本币金额只读；选择同公司同供应商同币种成本，显示逐项金额、合计及差额。
2. 删除“确认挂接”时把账单originalAmount/localAmount复制给systemOriginal/systemCost并把diffOriginal/diffLocal清零的处理。挂接只保存选中成本关系，当前成本从真实选择样例汇总，差额按双方各自金额计算。
3. 无来源、重复选同一成本、跨公司／币种不符时不能普通挂接成功；有金额差异允许完成来源关联，但状态只能待核对／差异处理中，不表示一致或已生成付款。
4. 核对抽屉按原账单→我方成本→差异原因→处理依据组织。无差异按原流程；有差异可“退回业务核实”或“引用已批准调整”，不能财务直接改采购事实抹平。
5. 退回需涉及费用项、原因与经办人；引用批准调整需选择含金额、范围、审批号及状态的明确记录，不用一个已批准勾选框代替证据。
6. 原成本、批准增减、当前成本和余差分别保留；待批记录不参与金额。核对完成后详情保留采用版本与依据，存在未处理差额不显示全部一致。
7. 避免通用保存分支在挂接时顺手覆写只读账单字段；同次确认不可重复应用调整或重复形成应付结果。本项独立实施，不等待对账发起方向确定。

必须通过的业务例子：
- 80000成本挂接82000账单后成本仍80000、差2000。
- 引用批准增1000后原80000、调整1000、当前81000、差1000；待批1000不计入。
- 无批准不允许改账单合计清差；同额正常例可核对，跨公司候选不可误选。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

<a id="p16"></a>
### P16｜财务 → 成本应付 → 付款申请｜公司协议与历史责任保留核验

状态：已完成，仅保留核验。主页面：[finance-payment-apply.html](../merchant/finance/finance-payment-apply.html)。

```text
任务：P16 公司协议与历史责任保留核验（主方案P16，核01）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。
本条为已完成成果的保留核验；默认不修改页面，只有发现既有承诺能力退化才作最小修复，不重新建设。

菜单：财务 → 成本应付 → 付款申请
主页面：merchant/finance/finance-payment-apply.html
当前代码定位（先核实最新版本）：公司协议与付款责任依据；shared/supplier-company-agreements.js
角色与交接：财务审核申请的采购及付款责任，出纳另在付款执行办理。

本次具体执行：
1. 只核验已完成的核01，不重建付款申请。采购公司、供应商、所选协议、付款责任及申请所涉成本对应，实际付款账户公司不替代采购责任公司。
2. 起算事件、已知起算日期、账期天数和应付日期有对应依据；缺起算日期保持待核实，不能按今天自动补。
3. 历史业务引用的到期协议可查，不自动替换续签版；新业务不能普通选择待批／失效协议。
4. 提前付款提示财务判断，不因未到期自动拒绝或批准；代付以已有批准为依据，不据样例放开所有公司。
5. 已满足只补本次核验记录；若退化只修该点。P21/P22负责实际付款及回单，本页不扩执行状态或新字段。

必须通过的业务例子：
- 北京出票后30天、上海账单确认后45天分别正确；缺起算记录不编应付日。
- 到期旧协议的历史成本申请依据仍在，换公司不能串协议。
- 付款申请通过不等于银行已付款。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

<a id="p17"></a>
### P17｜财务 → 收款应收 → 到账流水｜待核对申请与同笔真实到账关联

状态：待执行。主页面：[receipts.html](../merchant/finance/receipts.html)。

```text
任务：P17 待核对申请与同笔真实到账关联（主方案P17，核11）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。

菜单：财务 → 收款应收 → 到账流水
主页面：merchant/finance/receipts.html
当前代码定位（先核实最新版本）：已有手工登记、查重、识别／关联处理
角色与交接：出纳核实银行到账并关联销售申请，收款会计再确认订单分配。

本次具体执行：
1. 保留手工登记、导入与同账户流水查重，不新建银行流水池或真实银行连接。
2. 在现有识别／关联区补“关联待核对认款申请”选择；显示P12原申请号、付款方、公司账户、申请金额、付款日期及交易／凭证依据，与当前已核实流水并列核对。
3. 只有财务核实的真实到账样例可在本页登记为到账；销售上传凭证仍是待核对申请，不因此创建已收银行记录。交易号缺失时保留待核查，不能编真实交易编号。
4. 关联按账户、公司、币种及交易依据确认，金额相同仅是候选提示，不能自动判同款。金额不同可保留差异交认款复核，不覆盖原流水或原申请额。
5. 同一交易手工登记后再出现导入样例，复用既有查重，显示原流水及已关联申请、已认额，提供查看原记录；不能再造一份可认资金。
6. 无到账时申请仍待核对或退回补资料，不为了走通页面强造到账。关联仅表达本笔资料关系，不自动完成认款或应收核销。
7. 本页独立样例与P12/P18一致即可，无跨页同步要求。

必须通过的业务例子：
- 先有10000待核对申请，后核实同笔10000并关联；再次出现同交易只保留一笔原款。
- 同金额不同交易保留两笔，不误合并；不同公司／账户不可普通关联。
- 实到8000与申请10000保留2000差额；无到账不增加实际收款。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

<a id="p18"></a>
### P18｜财务 → 收款应收 → 认款复核｜到账证据和本次分配双重校验

状态：待执行。主页面：[finance-matching.html](../merchant/finance/finance-matching.html)。

```text
任务：P18 到账证据和本次分配双重校验（主方案P18，核11）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。

菜单：财务 → 收款应收 → 认款复核
主页面：merchant/finance/finance-matching.html
当前代码定位（先核实最新版本）：confirmReview、核销明细、reviewAction及差额处理
角色与交接：收款会计核实到账和归属，再确认分配／核销，剩余款沿现有去向处理。

本次具体执行：
1. 保留原复核抽屉、退回、预款和保留待认分支；同时展示销售申请与已核实原款。原款尚未核实时，只能继续核对或退回，不能普通确认后宣称已核销。
2. 分配明细逐订单显示应收、已认／已核销、本次认领和本次可核销金额；摘要显示原款、历史有效分配、可用额、本次合计及剩余。
3. 在提交函数而非仅提示区校验：本次有正数、逐行无负数、总额不超原款可用、不能重复订单分配，订单超收不得把待收算负。
4. 超出订单可核销的部分通过现有差额去向处理；保留待认不等于款项凭空消失，也不生成第二次收款。预款等去向沿已有批准条件，不扩大范围。
5. 确认只对本次有效分配生效并留确认记录；后续再次认领仅用剩余额。同一确认重复点击不重复核销，已确认部分不能被退回其他部分清除。
6. 退回保留原申请金额、未生效分配及意见，不能把差异原因写到金额字段当金额；当前差额按金额继续可核对。
7. 结果明确已确认分配及余款，原页面若生成凭证类提示也不能冒称真实NC入账，不改NC或应收页面。

必须通过的业务例子：
- 原款10000确认6000+2000，已分配8000、余2000；第二次最多2000。
- 6000+5000即使进入最终确认也拦截；未核实到账不能完成认款。
- 退回不清除已确认部分；同一确认连点仍只记一次。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

<a id="p19"></a>
### P19｜财务 → 收款应收 → 转款处理｜转款退回与冲正保留原记录

状态：待执行。主页面：[finance-transfer-records.html](../merchant/finance/finance-transfer-records.html)。

```text
任务：P19 转款退回与冲正保留原记录（主方案P19，核13）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。

菜单：财务 → 收款应收 → 转款处理
主页面：merchant/finance/finance-transfer-records.html
当前代码定位（先核实最新版本）：confirmWork的confirm/difference/reverse；targetPendingAfter
角色与交接：财务确认款项从原订单分配到目标订单，银行实收总额不变。

本次具体执行：
1. 原详情分别展示原／目标订单收款公司、客户、门店、当前应收与分配；原银行流水及P09-D申请金额独立保留，不只显示一个公司。
2. 可转金额按原收款扣已退、有效已转及其他在途占用；复核本申请时不要再次扣本申请自身占用。确认金额正数且不超可转及目标可承接额。
3. 跨公司、不同币种、跨客户等不能以通用确认直接绕过；沿现有批准范围核对，无明确依据保留异常。不得新增自动审批／自动划拨制度。
4. 退回补充或关闭：保留原申请4000，实际转款0，保留理由、时间及历史；删除把申请transferAmount写成0来表示未执行的做法。
5. 成功转4000时按目标原待收6000计算本次后待收2000，不能默认targetPendingAfter为0。保持银行原款10000不增加。
6. 冲正新增关联原成功记录的反向处理4000、原因和批准依据；原成功4000继续可查，不能把原成功额直接改成负数或删记录。
7. 重复确认／冲正不能多次改变分配；冲正额度不超原成功尚未冲正范围，目标存在后续使用则保留需核对的限制，不机械恢复成申请额。
8. 页面能查看申请、确认、退回和冲正各自结果，不要求真实跨页回写或产生银行交易。

必须通过的业务例子：
- 10000扣已退2000、已转3000，可转5000；申请4000退回：申请仍4000，实转0。
- 成功转4000，目标6000待收变2000；银行实收不变。
- 无后续使用样例冲正4000恢复目标待收6000，原成功和冲正同时可查；重复冲正不再变。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

<a id="p20"></a>
### P20｜财务 → 收款应收 → 退款执行｜退款事实独立、失败重试留历史

状态：待执行。主页面：[finance-refund-execute.html](../merchant/finance/finance-refund-execute.html)。

```text
任务：P20 退款事实独立、失败重试留历史（主方案P20，核10）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。

菜单：财务 → 收款应收 → 退款执行
主页面：merchant/finance/finance-refund-execute.html
当前代码定位（先核实最新版本）：renderReceivable；执行、回单与重新退款处理
角色与交接：出纳执行批准退款，业务应收调整由原批准记录决定，供应商追款另行处理。

本次具体执行：
1. 保留原退款、回单和失败处理入口。在依据区逐笔展示批准退款、原收款、已退、有效在途及本次可退；多次退不超原款剩余，不能按合同标价无限退款。
2. 删除renderReceivable根据退款主状态推“已冲销”的逻辑；关联应收只引用真实给定的调整单、批准状态及结果，没有依据显示待核实，不能推断。
3. 实际退款记录分执行金额、日期、交易依据、确认人和资金结果；受理、待结果、待回单、退回补充或关闭均不能单凭名称当成功。已核实成功与回单是否补齐分别表达。
4. 明确失败后重新退款创建新执行尝试，原执行号、金额、时间、流水和失败原因保留；累计已退只累加已核实成功记录，同一成功不因后补回单再次计款。
5. 结果未知在原详情补“核查结果”处理；未核实之前不能普通重试。核实未退后才允许新尝试，核实已退则记录该次成功，不能另外造一次退款。
6. 原申请应退、累计已退、剩余待退可核对；退回补资料不抹掉已成功退款或原应收调整。供应商追回只在关联售后／追款记录查，不因客款完成清余额。
7. 不接银行、不自定退款优惠、先退或资金成功证据制度；用明确已批准／已核实的独立样例表达。

必须通过的业务例子：
- 应收减5000已批准、退款申请退回：应收调整仍成立，实际退款0；缺调整依据不显示已冲销。
- 5000首次失败、第二次成功：两次记录、累计退款5000；补回单不变金额和支付日。
- 未知结果不能再付；确认未退后才可重试；已退余额不足不能超退。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

<a id="p21"></a>
### P21｜财务 → 成本应付 → 付款执行｜实际付款与回单、重试分别记录

状态：待执行。主页面：[finance-payment.html](../merchant/finance/finance-payment.html)。

```text
任务：P21 实际付款与回单、重试分别记录（主方案P21，核10）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。

菜单：财务 → 成本应付 → 付款执行
主页面：merchant/finance/finance-payment.html
当前代码定位（先核实最新版本）：confirmWork：提交银行、登记已付款、上传回单、重新付款
角色与交接：出纳按已批准申请付款，财务随后核对回单；支付受理不等于资金成功。

本次具体执行：
1. 保留申请、成本或预付、采购公司、付款责任、金额和账户依据；历史协议原样引用，不在执行时重新选一套协议。
2. “提交银行”只形成一次执行请求及待结果，不增加实际已付，不伪造银行成功流水。原型不真实提交银行。
3. “登记已付款”输入实际日期、实际金额、交易依据及确认人；只在已有批准的有效证据或明确已核实样例下计成功。申请额与实付额不一致要保留差异，不能只改申请总额。
4. 回单作为对应执行的材料状态，上传后转待核销；不新增资金发生，不把上传日改作支付日。原付款成功但缺回单者仍可查实际已付。
5. 失败重试保留第一次执行号、金额、时间、银行结果和原因，另建新尝试，删除清原bankFlow／执行依据后伪装同一次付款的处理。
6. 结果未知先核查，不能直接重新付款；同一申请剩余可付以已确认成功及有效在途核对，重复点击不重复记成功。
7. 列表保持一个主处理状态，回单／核销结果在详情或弱文本表达，不顺带改全表排版。实际核销由P22承接，预付退回由P24承接。

必须通过的业务例子：
- 申请60000，首次明确失败、二次成功：两次执行，实付60000而非120000。
- 9月10日付款、9月12日补回单、9月13日核销：付款日期仍9月10日，金额60000。
- 受理及未知不计成功；未知不能直接重试；同一次成功连点不加钱。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

<a id="p22"></a>
### P22｜财务 → 成本应付 → 回单核销｜核销不重复记付款

状态：复用核验，缺口才改。主页面：[finance-remittance.html](../merchant/finance/finance-remittance.html)。

```text
任务：P22 核销不重复记付款（主方案P22，核10）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。

菜单：财务 → 成本应付 → 回单核销
主页面：merchant/finance/finance-remittance.html
当前代码定位（先核实最新版本）：candidateRows、renderCandidateRows、confirmWork及差异处理
角色与交接：财务核销人员核对某次已发生支付的回单，不再次执行支付。

本次具体执行：
1. 这是复用核验加必要修正，已有正确区块保留。原处理抽屉显示付款申请、原付款及具体成功执行记录；失败尝试不能成为普通核销候选。
2. 回单金额、币种、公司与实际支付并列核对。回单59000、支付60000仍保留差1000，不能用回单金额覆写支付。
3. 确认核销只保存匹配／核对结果及核销日期，不能新增一次实际支付、改支付日期或再次减少应付。重复核销同一关系不能重复计数。
4. 退回补资料保留原实际支付和已有回单，只改变材料／核对阶段；不反向清掉已付、不调整原业务成本。
5. 差额使用现有处理入口及批准依据，资料不足保持待核，不点确认就归零；原主操作及正常匹配流程保留。
6. 只补本页配套样例和确有问题的处理；不实现P21跨页写回，不另造通用财务凭证流程。

必须通过的业务例子：
- 已付60000确认核销后仍60000，原支付日期不变。
- 回单59000保留差1000；退回补资料仍承认原60000支付事实。
- 重试成功的回单关联第二次成功，不挂第一次失败；重复确认不重计。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

<a id="p23"></a>
### P23｜财务 → 成本应付 → 预付管理｜待退回与实际预付余额分开

状态：待执行。主页面：[finance-prepayment-offset.html](../merchant/finance/finance-prepayment-offset.html)。

```text
任务：P23 待退回与实际预付余额分开（主方案P23，核12）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。

菜单：财务 → 成本应付 → 预付管理
主页面：merchant/finance/finance-prepayment-offset.html
当前代码定位（先核实最新版本）：applyBalanceResult及余额退回／余额处理
角色与交接：应付会计根据原预付与供应商责任申请退回，出纳核实到账后再反映实际退回。

本次具体执行：
1. 仅改余额退回及对应详情，不重建普通预付、多团分摊、已存在冲抵。
2. 原预付公司、供应商、币种、原付款、账面余额、已冲抵和已退只读；选择关联售后／供应商责任确认，输入本次退回申请额、原因和依据，不能超责任及可处理范围。
3. applyBalanceResult不能提交申请就refreshAfterBalance并把实际balance减掉，也不能直接生成已到账或凭证／NC待推送结果。申请形成待退回记录，实际预付余额保持。
4. 分开申请额、已实际退回额、待退额及剩余原预付。待退是否冻结可用余额按已批准规则；没有批准不把可用额当已确认结果，保留待财务核定，不擅自选择全冻或全可用政策。
5. P24已核实到账的独立样例带退回记录、原流水、金额和日期，才影响实际已退；无需跨页自动同步。未到账不能结清。
6. 同意以后抵费用只是约定，尚未实际冲抵仍为未结；实际冲抵保留原单依据，与现金退款分开。
7. 结清按实际冲抵、实际退回及其他获批处理核对；客户退款完成不决定预付结清。重提同一责任不能重复计申请。

必须通过的业务例子：
- 原预付10000、确认申请退8000：提交未到账实际余额仍10000，待退8000。
- 实退5000后实际未结余额5000，其中待退3000、其余原预付2000，各有来源。
- 仅约定抵未来费用不标已到账；超余额／超责任申请拦截，未实退不结清。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

<a id="p24"></a>
### P24｜财务 → 成本应付 → 付款退回｜未到账及部分退回持续核对

状态：待执行。主页面：[finance-payment-return.html](../merchant/finance/finance-payment-return.html)。

```text
任务：P24 未到账及部分退回持续核对（主方案P24，核12）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。

菜单：财务 → 成本应付 → 付款退回
主页面：merchant/finance/finance-payment-return.html
当前代码定位（先核实最新版本）：现有匹配到账、确认退回、冲销及详情处理
角色与交接：应付会计接收应退责任，出纳核实实际到账并对应原付款。

本次具体执行：
1. 保留原匹配、确认与冲销，新增或复用当前页待退回记录入口。允许有P07/P23确认责任但尚无银行到账号的事项进入待退回，不能强迫先填假流水。
2. 原对象资料为售后、成本／预付、原付款、公司、供应商、币种、确认应退及依据。区分供应商退款、预付退回、银行原路退回，不把银行付款失败当成本减少。
3. 匹配实际到账选择已核实流水，核对公司、币种、原款性质、本笔可关联余额；输入本次实际退回额。不是新销售收款，不在订单侧增加成交实收。
4. 每次确认增加对应实际退回记录，累计已退与未退可复算；应退8000、本次5000后仍余3000。多次到账各留流水、日期和金额，不覆盖上次。
5. 同一笔到账不得超用／重复全额关联；退回总额超责任时保留多到款差异交现有财务处理，不自动重写应退额使其相等。
6. 实际抵扣或冲销按原款性质及批准依据记录；不将所有退回自动减采购成本，也不在确认到账后再冲减原付款一次。
7. 详情能区分待退、部分退回、已处理；客户退款结束不自动关闭本项。正式负应付等单据类型未定只保留财务核对结果，不新增生产单据引擎。

必须通过的业务例子：
- 应退8000且未到账可保存待退；到5000后已退5000、待退3000，再到3000才完成。
- 重复关联同5000不能累计成10000；不同两笔款可分别登记。
- 银行退回和供应商减费退款结果不混，原成本不因任一到账自动减少。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

<a id="p25"></a>
### P25｜财务 → NC推送｜凭证校验统一阻断不平推送

状态：待执行。主页面：[finance-nc.html](../merchant/finance/finance-nc.html)。

```text
任务：P25 凭证校验统一阻断不平推送（主方案P25，核14）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。

菜单：财务 → NC推送
主页面：merchant/finance/finance-nc.html
当前代码定位（先核实最新版本）：NC-JS-20260624001、markPushed、moveFixedToQueue及批量推送
角色与交接：核算人员核对完整会计凭证，校验后交集团财务核算系统NC，不用业务毛利凑凭证。

本次具体执行：
1. 在原凭证详情分开业务结算结果与待推会计分录；每条借贷及合计、差额按明确分录数据计算，不从“校验通过”文案推断合格。
2. NC-JS-20260624001借297800、贷441600差143800，必须校验不通过；保留原业务来源，不能补无依据科目或将毛利当平衡项。相同旧算法生成的其他结算／项目示例同样校验，不只特判此单号。
3. 单行、详情、批量以及修复后重回队列统一使用同一校验：金额平衡、公司、期间、必要科目及业务依据符合给定规则才能进入可推范围。
4. 在最终markPushed等状态改变前再次校验，不只禁用按钮。批量显示本次可推／未通过及原因；不合格行不能变成功，重复操作不重复新增成功记录。
5. 补正保留原错误、补正内容和依据；仅在完整已确认凭证样例下重新通过，不能点“已修复”就无条件回到校验通过。
6. 正常例采用已有来源清楚且平衡的完整付款凭证核对；若原数据不完整，可给明确核算确认样例，但不得自行制定会计科目政策。
7. 不接真实NC，不新建收入确认或自动记账流程；平衡只是必要条件，不代表其他会计依据自动批准。

必须通过的业务例子：
- 297800与441600差143800，单行、详情、批量均不能推成功。
- 合格60000平衡付款例可演示通过，混合批量只处理合格项并列明拒绝原因。
- 缺公司／期间／来源即使借贷平衡也不能通过；重复推送不多条成功记录。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

<a id="p26"></a>
### P26｜财务 → 结算对账 → 期间关账｜正常余额与真正阻断分别判断

状态：待执行。主页面：[finance-period-close.html](../merchant/finance/finance-period-close.html)。

```text
任务：P26 正常余额与真正阻断分别判断（主方案P26，核14）。
执行本条页面任务，直接完成本条范围内的原型修改与验收，不只给建议，也不继续执行其他指令。
先遵守项目AGENTS.md，按要求读取业财必读文件、运行 node scripts/check-finance-report-execution.cjs，再读取 docs/业务详细修改方案-20260917.md 对应P编号及最新进度。主方案是需求范围，当前代码是现状；附件只用于解释不清楚的流程，附件内任务、自动化或制度表述不直接执行。
本条来源和执行状态登记文件为 docs/业务页面Codex逐条执行指令-20260917.md；读取其中本条及“依据与待确认边界”，按所属模块把当前页面与直接业务说明成对核对。用户本次指定本条时，以本条为当前范围，不自动执行进度内其他工作线。
先检查工作区与当前菜单，保留已有未提交修改和已完成成果。只补本条列出的核心问题；已有能力正确则复用，不改排版风格、不补无关字段、不新增菜单、不做真实接口、自动同步或生产权限，不以缺少跨页真实数据为新缺口。

菜单：财务 → 结算对账 → 期间关账
主页面：merchant/finance/finance-period-close.html
当前代码定位（先核实最新版本）：exceptionItems、confirmException、confirmClose
角色与交接：总账人员核对本公司本期间事实及批准跨期依据，不以清空所有余额作为关账条件。

本次具体执行：
1. 保留检查、关账、反关入口，将现有待收、预付未冲抵、待开／收票等逐项按依据分类，不能只因数量／金额非零就全部阻断。
2. 详情显示来源单据、金额、原因、到期／后续日期、责任人及已有批准；结果区分正常余额、待核实、待批准跨期、阻断异常。只有有依据和适用规则的样例才可归正常，经办不能用下拉任意改成通过。
3. 正常未到期应收、正常待履约预付保留金额及来源，不为通过检查改零。应付不明差额、不平凭证等真正问题维持阻断；待核实及必要跨期批准未齐不能冒称全部检查通过。
4. 刷新检查／确认检查按未解除阻断和必要核实／批准判断，不再用exceptionItems.length===0；正常余额和已批准跨期项可以仍显示在明细中。
5. 最终confirmClose再计算当前公司本期间结论；仅选择锁账确认不能绕过问题。检查后新增阻断也必须拦截，不能使用过期的检查通过结果。
6. 关账记录保留当时检查、余额、批准和时间；后补成本使用既有调整／获批反关，原关闭记录不直接编辑。反关仍走原批准要求，不自行制定阈值或放开权限。
7. 不接真实NC和账簿服务；跨期、待票及重大性政策未批时保留待确认，采用有明确批准依据的独立样例验正常路径。

必须通过的业务例子：
- 依据完整未到期月结应收可按给定已批准分类正常保留，不要求先收齐。
- 正常预付10000与无法解释差额10000分类不同，后者未解决不能关账。
- 仅正常余额／已批准跨期可继续；缺批准或不平凭证即使选锁账仍不能完成。
- 检查后新增阻断，最终确认拦截；已关账原结果与后续调整记录分开。

共同交付要求：
- 修改仅限本条主页面与确实直接依赖的共享脚本／局部样式、对应业务说明和验收记录；共享文件必须限定作用范围并核验直接引用者，不顺手改其他模块。正式政策未定只限制受影响分支，继续完成其余明确功能，未定规则不得以默认值批准。
- 入口与处理必须可实际演示：当前对象正确带入、控件可填写／选择、必填与额度校验有效、取消不改原数据、提交后当前页能查看本次结果；有金额变化或批准结果时保留原记录。原型用一致独立样例即可，不要求跨页面取数或回写。
- 按上述例子验证正常、异常、取消／重开和重复操作；计算变更用独立算例核对，并实际点击本次入口检查结果、控制台及金额／按钮完整可见。不能浏览器验收时如实记未验，不把静态检查当通过；不要求本次保存图片。
- 运行适合本次改动的语法、业务计算及直接回归检查；已通过且无新问题不扩大为全系统测试。更新本指令执行状态、对应验收记录、docs/财务要求-现有功能-缺口-修改对照表.md及docs/业财报表自动化改造进度.md第三、四节；涉及原业务说明时同步其有效规则。docs/集团财务报表执行清单.md仅补说明，原8批／36项／6场景不重开、不新增批次。
- 收尾运行 node scripts/check-finance-report-execution.cjs 和 git diff --check。回复具体改了什么、实际验收结果、待确认或未验内容，以及建议下一条编号；本条结束，不自动做下一条。
```

## 核心需求覆盖核对

| 主方案核心项 | 本文执行指令 | 处置 |
|---|---|---|
| 核01 公司协议 | P01、P02、P16；P07各项保留 | 已完成，不重复实施 |
| 核02 混合签证 | P03、P09-A、P06 | 产品允许、订单逐人、实际代办分别完成 |
| 核03 原渠道退位 | P08-B、P04-A、P10-A | 渠道占用与归还保留原依据 |
| 核04 已售团核心变更 | P04-B、P10-A | 原团保护、目标团期及售后承接 |
| 核05 采购减量 | P05 | 三个菜单同页修改和验收 |
| 核06 外采确认 | P02保留、P08-A、P13、P09-B、P10-A | 前序完成不代表整组完成 |
| 核07 价格与优惠 | P08-C、P09-C；P09-B引用 | 原价、规则、营销、特批与供应补差分别处理 |
| 核08 个人分签 | P11 | 覆盖范围、合同金额及各份状态 |
| 核09 对账差异 | P15、P07-A、P14 | 双方依据、批准调整及余差 |
| 核10 金额与执行 | P07-B、P09-C、P10-B、P20、P21、P22 | 不双扣、实际资金与处理记录分别保留 |
| 核11 收款异常 | P12、P17、P18 | 待核对、真实到账、分配结果 |
| 核12 供应商待追回 | P10-B、P07-C、P23、P24 | 责任确认、待退及多次实际到账 |
| 核13 转款 | P09-D、P19 | 双方责任、原款、原申请与冲正 |
| 核14 核算关账 | P25、P26 | 校验不平、分类与最终放行 |

## 需求—现状—缺口—页面—验收索引

本节是主对照表的本轮明细附件。现状以最新源码及执行记录为准；未完成项的验收列是未来目标，不能当作页面通过记录。

| 业务／财务要求 | 已有能力与当前缺口 | 修改页面／指令 | 验收方式 |
|---|---|---|---|
| 公司协议责任准确 | 协议脚本及产品、成本、付款依据已完成 | P01/P02/P16核验；P07保留 | 北京30天／上海45天；历史到期依据保留 |
| 逐人签证且不多建任务 | P03已支持产品两方式并存；订单共享脚本仍统一代办 | P03已完成；P09-A/P06待执行 | 三人只有两条代办，自办不减价不抹已送签 |
| 库存归还来源可追 | 释放只累加团期总available，未逐笔保留渠道；编辑直接覆盖及监听提前改方案 | P08-B/P04-A/B/P10-A | 代理可用4→6、直营不变；取消编辑原值不变 |
| 采购减量不丢0及分配约束 | saveChangeOrder以主单价算全部、零值回退、仅单规格更新且直接生效 | P05 | 100→80余20；100→50缺10；多规格28000；0不回原额 |
| 外采参考数不等于确认 | P02已完成，预订有通用待确认；供应商抽屉固定对象且回复只关闭 | P08-A/P13/P09-B/P10-A | 四回复有独立结果，无位及补差由原角色处理 |
| 优惠成交留批准依据 | 下单currentDiscountUsages按金额／渠道给固定优惠，调整已有 | P08-C/P09-C | 10000→9500→9200不重扣，未批不生效 |
| 个人分签不多算订单 | 原合同生成、审核、发送已有，无完整多份覆盖结果 | P11 | 三份各10000、订单仍30000，2签1未签不全签 |
| 对账不覆盖成本 | confirmWork挂接复制账单到成本并清零差异；供应商端未绑定当前账单 | P15/P07-A/P14 | 80000与82000挂接差2000，增1000后差1000 |
| 经营金额与资金事实分开 | settlementIncomeRows按比例造订单，settlementTotals混扣；退款依主状态推冲销 | P07-B/P09-C/P10-B/P20 | 应收95000、累计收100000、已退5000、净收95000 |
| 资金执行与回单不重复 | 付款登记与回单状态混用，重试清原流水；核销能力已有 | P21/P22；P20退款复用原则 | 失败后成功实付60000，回单后补不增款不改支付日 |
| 认款有来源与额度 | 查重、人工登记、复核已有；上传固定匹配，提交及复核缺硬校验 | P12/P17/P18 | 10000分8000余2000；11000拦截；无到账不计收 |
| 退回原款与追款责任保留 | 预付applyBalanceResult申请即改余额；退回页已有匹配，缺未到账责任 | P10-B/P07-C/P23/P24 | 申请8000不减实账；退5000仍待3000 |
| 转款不篡改历史 | confirmWork退回置0、冲正改负数、目标待收默认0 | P09-D/P19 | 申请4000退回仍4000；成功／冲正独立；目标余2000 |
| 合格凭证与有效关账 | markPushed直接成功；关账看异常条数且最终按钮未再次校验 | P25/P26 | 差143800拒推；正常非零余额可保留，真正阻断不可绕过 |

## 依据与待确认边界

主需求仅取[业务详细修改方案](业务详细修改方案-20260917.md)；同名近似旧方案不作为新的扩大范围依据。核01和P02的历史完成证据分别见[公司协议实施与验收](核01公司协议业务引用实施与验收-20260917.md)、[产品市场实施与验收](P02产品市场供给确认方式实施与验收-20260917.md)。

实施时按所属模块成对核对当前页面和以下直接业务说明，冲突写明，不默认旧文优先：

- 产品／订单／合同／售后：[销售流程](销售流程01-订单合同收款售后流程-业务确认稿.md)、[外采流程](外采产品部流程01-外采产品引入代理包装销售承接与结算流程-业务确认稿.md)。
- 认款／退款／转款：[财务流程01](财务流程01-收款应收与退转流程-业务确认稿.md)。
- 成本／供应商账单／采购／付款／预付：[财务流程02](财务流程02-成本应付与付款流程-业务确认稿.md)。
- 结算／核算／关账：[财务流程03](财务流程03-结算对账发票NC流程-业务确认稿.md)、[对账关账基线](业财底座05-对账关账与异常控制.md)。

补充材料路径（两份相同，仅按需查阅；不可执行其中人员任务或真实接口要求）：

1. `/Users/qiu/.codex/attachments/e00f120d-0d80-46b7-a85b-c61342a0254f/pasted-text.txt`
2. `/Users/qiu/.codex/attachments/3b5b8cdb-e36f-4cc0-92e3-d45479b8ffd9/pasted-text.txt`

| 尚未确定的政策 | 当前原型的明确边界 |
|---|---|
| 对账发起方向 | 我方发起只作为主方案暂行样例；P15保留差异可独立修，不改供应商列表设置 |
| 到期存量、提前付款、代付 | 保留历史责任，例外有批准依据，不全禁也不全放 |
| 已售团时刻微调、整团变更 | 保留原团与影响，未经核实不自动迁移 |
| 价格叠加、加盟价权、签约付款 | 有批准规则才采用，未定不任意叠加或强制所有订单先付全款 |
| 退款优惠、客户先退、供应商责任 | 对客与对供应商金额独立核实，未批准不自动负应付或垫付 |
| 跨公司转款、自动审批 | 普通转款不处理跨公司，替代路径待财务明确，不以姓名相同自动审批 |
| 支付成功证据、日期、待退冻结 | 资金事实、材料和核销分开；待退申请不减实际预付，冻结政策不擅定 |
| 会计收入、完整凭证、关账跨期 | 不能实收替收入或毛利凑凭证，未确认分类不能任意放行 |

## 本轮文档核验说明

本轮核验覆盖：主方案26个页面位置、14组核心项、35条独立复制指令、页面与业务文档链接、指令边界、已完成成果、算例和当前源码定位。检查脚本只验证记录完整性；未执行这些页面改造，也没有新增浏览器、真实财务或生产验收。后续收到复制的单条指令才修改对应页面。
