# 订单详情最小调整验收记录

日期：2026-09-15。用户在销售下单页确认后要求“改下一个”，本次执行逐页最小调整第5页，只修改 `merchant/sales/orders-detail.html`。

销售人员从订单列表查看本单，核对确认条件、释放保留名额或申请增人；计调负责资源及人数确认，财务负责认款与补款条件。资源、收款、合同、售后仍在各自原模块处理。

| 已确认要求 | 原页面缺口 | 本次调整 | 验收 |
|---|---|---|---|
| 分开确认条件 | 原订单信息只有固定预留到期，缺少授权、资源、付款及名额判断 | 原订单信息网格补销售授权、资源确认、付款条件、名额状态、保留截止及到期处理；不新增分区 | 字段、原卡片宽度及样式核对 |
| 转确认有条件 | 原操作选择原因后只弹出成功提示，且混入关闭订单的财务影响选项 | 原抽屉只读展示条件和处理岗位，检查订单状态、门店资质授权、资源、有效保留和本单认款要求；通过后当前页变已确认并记确认时间及日志 | 缺条件、暂停、到期、授权失效、已释放及满足定金／无需先付款等测试场景 |
| 释放不能误取消订单 | 无释放检查和可见状态结果 | 未付款且无签署、出票保护事项的预留／占位可人工释放；订单保留为待确认，名额已释放。已收款、签署中／已签署或已出票需售后处理；暂停不阻止合法释放历史名额 | 普通释放成功及三类保护事项拦截、日志核对 |
| 增人有资源与申请边界 | 没有增人入口及检查 | 原更多菜单补增人，复用原操作抽屉；核对主状态、有效授权、期限、整数人数及计调确认的可增人数；成功为待计调确认申请，进入原售后记录和日志，不立即改原人数或金额 | 整数、余量、未知余量、重复申请与记录检查 |
| 原UI保留 | 无需整页重构 | 保留原7个Tab、头部、网格、按钮及抽屉样式；确认条件网格用原通栏表单容器，抽屉内容用自然高度容器避免文本裁切及空白拉伸；不改共享CSS | 页面截图、宽度字体间距、确认及增人抽屉显示检查 |

另外删除本页旧24小时自动释放及授信文字，与用户已确认范围一致；操作按钮按订单主状态决定，不再把收款状态文本混入判断。

22项浏览器功能检查通过，无脚本错误；另核对本地文件加载、确认抽屉信息完整显示和1024像素窗口增人抽屉无横向溢出。内联脚本语法及差异格式检查通过。临时证据在 `/tmp/caesar-store-step5/`。

边界：沿用当前原型门店资料及历史订单示例，未接真实审批资料。资源、付款、可增人数、到期处理无明确记录时展示待确认并拦截相关操作；保留截止已有历史示例日期不代表制定统一时长。正向验收中的资源已确认、付款节点与可增人数仅为测试条件，未写为已批准业务规则。动作和增人申请为当前页演示，刷新重置，不自动回写跨页名额、金额、合同或财务单据。本次未扩展已有取消、作废和其他售后动作。

唯一下一步：用户确认订单详情页后，再按指令处理门店预存余额页。
