# 销售下单最小调整与收付款菜单验收记录

日期：2026-09-15。用户明确要求“收付款配置菜单单独出来做二级菜单放在NC推送后面，同时执行销售下单页”。本次是逐页最小调整第4页。

销售顾问从产品预订或订单管理进入下单页，选择产品、人数、客户、来源与处理方式。门店档案和有效销售授权约束新增订单；下单页表达提交状态，资源确认及收款仍由原业务页面承接。

| 本次调整 | 实现与核对结果 |
|---|---|
| 菜单 | 收付款配置从资金票据下移出，成为财务二级菜单，紧跟NC推送；链接仍是原收付款配置页。只移动共享导航中的这一项，保留同时进行的数据中心菜单调整 |
| 处理方式 | 原下拉框增加草稿、待确认，原预留、占位、直接确认保留；删除固定24小时及永久占位表述，按钮随选择更新 |
| 草稿 | 未选产品、未填联系人也可保存；复用结果区域及原按钮样式，继续编辑返回原表单，保留填写内容。不占名额，不写入正式订单记录，不展示草稿的财务生成及后续处理区 |
| 待确认 | 保留必要资料校验，提交后状态待确认、资源未占位、确认时间为空；不能通过待确认绕过门店暂停、资质失效、授权及停售限制 |
| 提交检查 | 核对门店状态、资质有效期、销售授权生效、产品类型与目的地范围、当前可售状态；预留／占位另核对权限、期限、到期处理与余量；直接确认检查资源及本单付款条件 |
| 付款与资源 | 选择收款计划、预存扣款不视为已支付；普通团期按人数核对余位，缺少对应舱型／房型／铺位可确认余量时进入待确认处理，不能直接按人数推算房间。未增加财务扣款模拟 |
| UI | 保留原抽屉、分区、表单、费用汇总及共享样式；不新增说明卡、条件面板或流程区。结果区仅随草稿／待确认调整必要状态及操作 |

依据：`merchant/sales/store/index.html` 当前门店类型、可售范围与权限；`merchant/sales/store/detail.html` 资质日期示例；《门店开发疑问分类与答复-业务决策及产品确认-20260914》第4—6项。未使用已撤回整体改造遗留的门店共享规则去覆盖当前页面。

演示与待确认边界：门店资料是当前页静态示例，资质日期沿用门店详情的2027-04-30示例，不表示已核验各店真实资料，不与门店审批跨页回写。保留期限、释放方式及付款条件维持待业务确定；因此当前示例的预留、占位和直接确认会提示缺失条件，可选择待确认或草稿。草稿为当前页内保存与续填演示，刷新重置；没有实现草稿台账、后台保存或跨页恢复。待确认沿用页面已有的订单保存方式，没有新增库存、资金及合同自动联动。

本机Chrome及规则检查共26项通过：原尺寸、字体、间距一致；草稿空值与续填、待确认、必填、权限、期限、资源、付款、类型和目的地限制；非门店原流程；暂停、资质过期、授权未生效、停售、数量不足等异常；菜单唯一、与NC同级且紧跟其后、选中状态及本地文件加载。规则正向检查仅在测试中注入已批准期限／资源及无需先付款条件，未把测试数据写成业务政策。页面无脚本错误，内联脚本语法及差异格式检查通过。截图和输出在 `/tmp/caesar-store-step4/`。

唯一下一步：用户确认销售下单页UI和必要功能后，再按指令处理订单详情。本次不推进其他页面。

补充调整（2026-09-15）：按用户要求隐藏销售下单页“本单经营归属”整个模块，仅增加原区块隐藏属性，不改归属数据处理。浏览器确认模块不可见、订单处理仍显示、切换处理方式后保持隐藏且无脚本错误。唯一下一步仍为用户确认下单页后再处理订单详情。
