# 四类预款与保证金原型验收记录

2026-09-14，原第3批执06至执08已验收。正式数据、历史权限和财务政策另行承接。

## 业务依据

资金/往来会计在财务报表选择“四类预款与保证金”；采购财务在供应商采购与返点选择“预付款占用”。上游是确认款项、余额变动、应付冲抵和退回凭据，输出占用查询，不新增收款、冲抵或退款办理入口。

核对了预收页、预付冲抵页、预存页及财务流程01第八部分、流程02第十三部分。流程区分实际付款、团期分摊和应付冲抵。旧预付页`balanceRows`按固定日期拼接历史，并把当前待处理金额列作本次冲抵；当前余额不能恢复任意历史。源业务页不改，新报表隔离其推算结果。门店充值确认、冻结和可用依旧由原办理页负责。

## 财务要求、缺口与修改验收

| 编号 | 要求与缺口 | 修改 | 预期与实际 |
|---|---|---|---|
| 执06 | 四类款项方向不同，申请不等确认，冻结不是减少账面 | finance-reports新增prepayments主题及四视图；旧prepay替换；共享finance-prepayments-model/view | 预收10000+5000-6000-1000=8000，冻结2000、可用6000；预存余额24000、冻结3000、可用21000；保证金支付40000、收取30000分别统计，均符合 |
| 执07 | 分摊不等冲抵，跨期退回和缺历史须保留 | 期初/增加/使用/冲抵/退回/转款/批准损失/期末、历史、长期占用和应退未退；有当日凭据才列未履约占用 | 预付300000分摊200000不减余额，冲抵60000和退回10000后230000，应退未退20000；10月退20000后210000，9月仍230000；9月30日未履约占用80000，其他日期缺确认不猜 |
| 执08 | 供应商资金占用缺失，另写算法可能与财务不同 | 供应商页面补占用视图，直接复用同模型/查询与输出，不改变原采购/返点 | 同供应商S1、同公司/期间，CNY230000与EUR800分别列示，与预款报表一致；长期未结也包含应退未退，缺历史单列 |

正式来源默认待接入；独立验收资料显式切换。保证金按收取/支付方向分组，各类别不净额混加；缺历史、负余额、冻结超余额、同号冲突不提供可信可用金额。完全重复记录只计一次，尚未确认申请、期后记录保留依据但不计余额。无汇率不折算。选择风险不改变余额计算。25条分页附加算例验证全查询输出和文本防公式注入。

页面原路径保留：`merchant/finance/finance-reports.html?report=prepayments`、`merchant/data/supplier-reports.html`。只读组件`finance-readonly-report.js`统一查询、选列、排序、分页及完整依据导出；没有金额编号下钻。重复标题已收敛。

## 验收证据

- `node --test tests/finance-prepayments.test.cjs`15项通过；往来、预款、收退转付、供应商及执行保障合计90项通过。
- `NODE_PATH=/Users/qiu/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules node tests/finance-prepayments-browser.cjs`：file/随机HTTP共8组通过，错误0；含29条预付分页排序、全量依据输出、未提交条件隔离、异常保留结果、旧链接替代及供应商同额查询。
- 同环境`tests/report-local-preview-browser.cjs`30组、`tests/supplier-report-browser.cjs`6组通过。原采购/返点四视图、导出和重复导航保留。
- 桌面1440×1000与窄屏390×844截图已检查，表格内部滚动，短值未裁切。本地HTML不依赖8002；输出目录`/private/tmp/caesar-prepayments-qa`可由测试重建。`git diff --check`通过。

## 正式依赖

RPT-20待确认：期初和历史完整范围、冻结/释放与使用次序、款项类型及保证金方向、退回期限、长期占用分段和未履约分配凭据。历史、主体账簿、往来身份、实际确认及权限服务未接入；算例不能代替财务批准，不把原型通过称实账核平。预付分摊累计是业务使用去向，不代表已核销。

第3批完成后重读《集团财务报表执行清单》，继续第4批执09至执12。
