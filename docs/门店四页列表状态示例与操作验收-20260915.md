# 门店四页列表状态示例与操作验收

日期：2026-09-15。用户临时要求门店管理、门店成员、预存余额、门店对账所有列表具备相应状态示例及对应操作。本次补页面已有状态和抽屉明细示例，保留原UI，不恢复旧版整页重构。

| 页面 | 主列表状态示例 | 对应操作 |
|---|---|---|
| 门店管理 | 草稿、待审核、正常、暂停 | 草稿继续编辑；待审核查看申请／撤回；正常销售设置／财务设置／详情／暂停；暂停销售设置／财务设置／详情／恢复 |
| 门店成员 | 正常、待生效、待审批、变更待审批、停用待审核、已停用 | 正常调整／停用；待生效详情／调整；审批中的三种状态查看申请／撤回；已停用仅详情 |
| 预存余额 | 正常、余额不足、充值中、扣款复核中、冻结／异常 | 前四类沿用详情／明细／充值；限制账户为详情／明细／查看限制，列表和详情均不提供新增充值 |
| 门店对账 | 待门店确认、有异议、待财务复核、财务退回、已确认 | 待门店确认核对；有异议查看异议；待财务复核和已确认仅详情；财务退回再提交。抽屉内同步限制可编辑字段和提交按钮 |

门店列表增加海沧待审核、集美草稿，并将东海门店设为暂停示例，共8条；同步状态颜色、筛选和条数。查看审核申请只读，撤回转草稿，草稿可保存续填及重新提交。成员待生效使用已有集团员工周敏；待审批使用已有集团员工陈晓，不新增集团员工或门店建人入口，共8条。

| 抽屉明细 | 状态及按钮 |
|---|---|
| 充值申请 | 草稿、财务确认中、部分确认、已入账、已退回、已撤回；每行可查看，草稿继续填写，财务确认中可撤回，退回／撤回可重新提交。部分确认和已入账不提供整笔撤回 |
| 扣款记录及余额明细中的扣款关联 | 待复核、待执行、扣款中、已扣款、已退回、已撤销、待核查；门店端均提供查看，复核执行仍归财务 |
| 冻结明细 | 冻结中、已解冻、已转扣款；均提供查看，不让门店自行确认解冻 |
| 余额变动和余额明细 | 已入账、已扣款、已解冻的记录可查看；待财务确认的充值留在申请记录，不表示余额已增加 |
| 对账的充值、扣款、退款转预存、管理费明细 | 保留原已入账、待财务复核及已扣款示例，每行补查看入口。已入账明细不强塞未入账状态或财务审核按钮 |

明细操作放在原状态单元格内，保留原表格列宽和控件样式；扣款关联去除多余旧操作列后统一六列。记录查看呈现当前单据数据；申请重提更新当前页申请状态，不增加账户余额。对账已确认／异议处理中／待财务复核时不可重复提交，有差异时阻止直接确认。

验收：16项浏览器综合检查通过，覆盖四页全部状态筛选均有数据、各状态操作、只读边界、恢复入口、充值撤回重提与余额不变、全部抽屉明细查看入口及脚本运行；另通过门店8条计数、草稿续填及部分确认金额完整显示检查。截图和输出位于 `/tmp/caesar-store-status/`。四页未修改共享CSS。

范围与遗留：这些是用于开发核对状态和操作的静态原型示例，不是完整可结算账簿；账户汇总保留原样，新增状态记录不进行逐笔余额回算。审批、实际扣款及解冻未模拟。预存余额本次保留当前页面五类筛选场景，开户、整户关闭及账户主状态与处理中事项的进一步整理仍属于原计划第6页，不因本次示例补充视为已全部完成。

唯一下一步：临时任务已完成，待用户核对；按用户后续指令继续原门店预存余额页的最小调整，不自动推进其他页面。
