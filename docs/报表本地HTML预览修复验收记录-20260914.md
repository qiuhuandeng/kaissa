# 报表本地HTML预览修复验收记录

## 一、本次范围

按用户四张加载失败截图，暂停新增供应商报表，先修复数据中心本地预览与直接入口。不调整报表字段、金额计算、财务政策、团期订单及业务源页面。

财务、经营负责人从数据中心核对订单、回团与经营结果；预算和口径管理仍在报表管理内。本次只恢复打开、查询、导出与切换，不新增财务确认或下游单据。

## 二、问题与处理

| 使用要求 | 修复前现象及原因 | 本次修改 | 验收 |
|---|---|---|---|
| 双击本地页面可查看报表 | 渠道经营、团期毛利、预算任务、口径与数据核对均复现截图报错；公共资料动态模块导入在本地文件方式下失败 | 四页先按普通脚本加载共同资料，再加载页面脚本；取消动态模块导入依赖 | 八页分别在全新浏览器会话直接打开，不依赖先进入其他报表；查询、导出和刷新正常 |
| 菜单进入与直接打开一致 | 菜单动态插入外部脚本，未保持声明的依赖执行顺序 | 共享导航保持未声明异步的外部脚本顺序；不改菜单和本地整页跳转方式 | 旧产品分析进入四个原报错页及总览，人为延迟公共脚本200毫秒、总览模型400毫秒仍正确加载 |
| 不限定8002端口 | 既往浏览器验收基于网络服务，未覆盖本地文件；8002只是预览服务地址 | 新增独立本地预览回归，临时网络服务由系统分配空闲端口 | 本地文件与本次随机端口54544均通过；临时服务测试后关闭，本地报表无需服务器 |
| 反复切换仍能查看 | 此前未覆盖本地打开后的菜单及历史导航 | 补充两种方式的菜单重复进入、前进、后退测试 | 八个菜单、重复进入及前进后退正常，无报表脚本错误和请求失败 |

之前的52组浏览器验收不代表已通过本地文件预览，本次补齐遗漏。不通过关闭浏览器安全限制解决问题，也不要求安装或启动后端。

## 三、页面入口

菜单：数据中心 → 报表管理 → 口径与数据核对。

- [报表管理／口径与数据核对](../merchant/data/report-management.html)
- [预算任务管理](../merchant/data/budget-targets.html)
- [渠道经营](../merchant/data/channel-reports.html)
- [团期毛利](../merchant/data/settlement-reports.html)

本地打开需保留项目原有目录关系，页面仍使用项目内共享脚本、样式和图标，不是可复制到其他目录的单文件。

## 四、验收证据（开发复核）

- 修复前Chrome直接打开四页均出现截图中的提示；直接导入公共脚本返回 `TypeError: Failed to fetch dynamically imported module`，请求失败 `net::ERR_FAILED`。
- `tests/report-local-preview-browser.cjs`：27组通过。八页两种协议独立打开、查询、导出和刷新16组，菜单与历史导航2组，延迟加载入口5组，四页390像素窄屏检查4组。无脚本错误、无请求失败。
- 既有九个计算与规则测试文件：109项通过。
- 既有八个报表浏览器测试文件：52组通过，统一指定 `REPORT_BASE_URL=http://127.0.0.1:8002`。部分旧测试默认8000而该服务返回空响应，明确测试地址后全量通过；未操作其他已有服务。
- 新增结果、八页两种协议截图及导出文件：`/private/tmp/caesar-report-local-preview-qa/`。四个原报错页桌面及报表管理窄屏截图已人工复核，页面不再是加载失败空白。
- 修改范围：四个报表HTML、四个对应脚本、共享导航的外部脚本顺序、新增预览测试及控制文档。导航已有菜单改造等其他工作保留。

运行新增测试：

```sh
NODE_PATH=/Users/qiu/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules node tests/report-local-preview-browser.cjs
```

## 五、遗留与下一步

本轮仅验证本机Chrome及八个当前报表，未声称所有历史页面和所有浏览器都完成本地兼容验收。数据仍为原型演示资料，正式收入成本、权限、取数与财务政策缺口不因预览修复而关闭。

本次加载修复完成；报表工作线唯一下一步恢复阶段4第6小批后半批P10“供应商采购与返点”基础版，按来源与规则核对后另批实施，不在本次继续扩展。
