# 门店成员最小调整验收记录

日期：2026-09-15。用户在门店详情完成后要求“执行下一步”，本次只修改 `merchant/sales/store/members.html`。

门店管理员在原成员列表和权限抽屉调整本店权限；人员身份引用集团统一组织架构已有的员工示例，组织归属调整进入集团端。保留原6列表格、筛选项和抽屉分区，不新增说明区或流程区，不改共享样式。

| 本次调整 | 实现与核对结果 |
|---|---|
| 员工来源 | 删除添加已有员工按钮和创建分支；通过集团端正在使用的共同员工资料按编号读取姓名、手机号、所属门店，原字段只读 |
| 组织调整 | 原门店身份区域内增加“调整组织归属”链接，带对应员工编号进入集团端现有申请；浏览器验证可定位郑华 |
| 本店权限 | 保留原角色、范围和权限控件；草稿可续填，提交后保存申请并进入待审核状态，不覆盖当前角色、权限、姓名或组织 |
| 撤回与查看 | 原变更入口查看申请，申请中只读；原撤回入口恢复申请前的权限状态，保留草稿；原待生效员工撤回后不会误变正常；已停用人员只读 |
| 停用交接 | 在原变更原因区域内按需显示接手人，候选来自已有正常本店成员，排除本人及已申请停用者；有在办订单或客户时必须选择有效接手人，没有候选时阻止提交 |
| 历史归属 | 申请及撤回均不改变原销售人员或历史订单；只表达拟接手人员，不自动跨页转移客户和订单 |

本机Chrome完成13项检查，无页面脚本错误。原6列表格列宽、字体、间距及背景与修改前一致；清理已取消的结算价规则文字后，相应行随文字减少自然收紧，未修改行高样式。筛选、分页数量、草稿、提交、只读、撤回、交接校验及集团入口通过。本地HTML加载、脚本语法和本页差异格式检查通过。临时截图及输出位于 `/tmp/caesar-store-step3/`。

边界：共用已有员工资料脚本，不修改该脚本或集团组织页面；审批批准及权限实际生效未在本页模拟，申请为当前页演示，刷新重置。其他页面本次未推进。

唯一下一步：用户确认门店成员的UI和功能；确认后再按指令处理销售下单页。
