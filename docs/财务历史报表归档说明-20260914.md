# 财务历史报表归档说明

日期：2026-09-14；对应执28、执30、执31。分类和引用审计完成后，只退出日常菜单，不删除或移动历史文件。

| 保留文件 | 定位 | 现行承接 |
|---|---|---|
| `merchant/data/performance-reports-legacy.html` | 历史经营分析 | 经营总览及月度经营损益 |
| `merchant/data/finance-dashboard-v2.html` | 历史方案二 | 数据中心现行10页 |
| `merchant/data/finance-dashboard-v3.html` | 历史方案三；原四Tab专项保留历史身份 | 数据中心现行10页 |
| `merchant/data/product-analysis.html` | 旧产品分析 | 产品经营分析 |

处置结果：四页加载`shared/report-archive.js/css`，页头与浏览器标题明确已归档，提供现行报表入口；保留原文件以维持文档和外部收藏的历史引用。共享导航保留旧文件的数据中心归属，但不恢复其日常菜单。读取已保存页签时清理这四个历史地址，新页面使用现行名称。

引用审计命令：

```sh
rg -n 'performance-reports-legacy\.html|finance-dashboard-v2\.html|finance-dashboard-v3\.html|product-analysis\.html' --glob '*.html' --glob '*.js' --glob '*.cjs'
```

审计结果：当前业务页面无指向四个旧页的业务办理链接；共享导航仅保留归属与历史页签清理。`channel-margin-browser.cjs`与`return-finance-browser.cjs`仍用旧产品页作为旧入口跳转的回归起点；本地预览和最终测试通过组合文件名覆盖四页。文档保留原设计与验收历史引用，文件不移动，链接无需批量改写。

旧财务查询参数`report=profit`在原财务报表入口跳转新月度经营损益；旧计算代码已删除，原文件继续承载六类财务报表。保留旧参数承接不等于保留旧利润口径。

复验：直接打开四个旧HTML应看到归档提示、所属数据中心导航与现行入口；日常菜单只有现行10页。最终浏览器测试覆盖本地和随机端口，延迟共享资料加载的旧入口跳转亦纳入回归。
