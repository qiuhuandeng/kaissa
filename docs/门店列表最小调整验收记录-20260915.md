# 门店列表最小调整验收记录

日期：2026-09-15。依据用户“保留原UI，只做必要功能调整，一页改完确认”及“执行第一步”的指令，仅修改 `merchant/sales/store/index.html`。

门店运营、公司管理员继续在原列表和配置抽屉维护门店；配置提交形成待审核申请，不立即替换当前有效配置。本页仅演示当前页面内的操作，没有扩展组织、审批、订单或财务页面。

| 本次范围 | 完成内容 | 验收证据 |
|---|---|---|
| 保留原UI | 保留原5列、筛选区、操作位置、抽屉分区及样式；未修改共享CSS和导航，未增加说明或下一步区域 | 修改前后列表位置、列宽、首行高度、字体和背景逐项一致；销售及财务抽屉截图核对 |
| 配置字段 | 删除价格口径、库存口径、授信额度、超额度限制及本页相关示例；结算安排仅单单结／月结，预存保留在原付款方式中 | 页面字段及源码检查；月结启用账期选择，缺账期不能提交 |
| 配置申请 | 在原抽屉保留草稿、查看已提交内容和撤回编辑；提交后当前销售范围、结算安排、支付商户及列表金额保持不变 | 销售与财务各自提交前后比对；销售草稿按门店区分，不串入另一门店 |
| 暂停与恢复 | 原暂停弹窗必须填原因，暂停后同位置显示恢复；恢复申请核对当前销售、财务配置，待审核时仍暂停，不能重复提交 | 原因空值、暂停、筛选、恢复申请及重复提交检查；余额和有效配置不随暂停改变 |
| 原操作 | 新增门店仍使用原表单提交审核，本地HTML仍可直接打开 | 新增行与状态检查，本地页面及抽屉检查 |

本机Chrome完成18项检查，无页面脚本错误；页面内脚本语法及本页差异格式检查通过。截图和检查输出位于本次临时验收目录 `/tmp/caesar-store-step1/`。

原示例只有“预存”而未区分结算安排的门店，结算下拉显示“请选择”，不推断为单单结或月结。原保留时长、管理费和账期数字继续是原型示例，未作为新确认的业务政策。

审批通过、生效及资质有效性核验由既定审批和档案功能承接，本页未仿真审批回写；恢复申请在本页停留于待审批，不直接变成正常。草稿与申请为当前页面内演示，刷新后重置。其他7页仍按撤回后状态保留，本记录不宣布跨模块流程完成。

唯一下一步：用户确认门店列表的UI和必要功能；确认后再按用户指令处理门店详情，不自动推进其他页面。
