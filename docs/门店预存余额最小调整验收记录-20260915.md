# 门店预存余额最小调整验收记录

日期：2026-09-15。用户授权执行逐页最小调整第6页。仅修改 `merchant/sales/store/predeposit.html`，保留原7列表格、筛选区、详情及充值抽屉的设计样式，不新增说明卡，不改共享样式或财务处理页面。

本页供门店查看预存账户、发起开户及充值申请、核对历史记录。门店资料及公司归属来自门店档案；财务负责实际开通、到账核验和入账，门店提交申请不等于开通或收到款项。

| 技术问题／需求 | 原页面缺口 | 本次处理 | 验收 |
|---|---|---|---|
| 未开户门店如何申请、撤回和退回重提？ | 只有已有账户及充值入口 | 原列表加入未开户示例，用原抽屉组件填写联系人、电话、币种和用途；支持草稿续填、提交、查看、撤回及补充重提 | 必填拦截；草稿保留内容；审批中只读；撤回可编辑；退回显示原因并沿用申请号 |
| 何时生成正式账户？ | 没有区分开户申请和实际账户 | 提交仅产生开户申请号，仍显示未开户，不生成正式账号和余额 | 提交后账户号为空、余额为0；财务开通留待财务页面处理 |
| 账户状态如何与处理中业务区分？ | 充值中、扣款复核中、余额不足混在账户状态中 | 主状态统一正常、未开户、已冻结、已关闭；充值／扣款进度及余额预警仍保留在原栏目 | 四类筛选均有示例，初始11条，原金额和处理中信息保留 |
| 冻结与关闭后如何操作？ | 缺已关闭示例和完整入口限制 | 整户冻结可查看限制，不允许新增或重提充值；金额冻结不等于整户冻结；已关闭保留历史，重新开户创建独立申请 | 列表、详情及重提入口一致；关闭账户重复点击不重复建申请，原账户不变 |
| 上传充值凭证是否就入账？ | 申请内容及凭证留存不足，重提可切换门店 | 校验金额、时间、打款户名及凭证；提交保留申请和文件名，等待财务确认；原申请重提锁定门店 | 上传、提交、撤回和重提均不增加可用余额或变更冻结金额；部分确认和已入账不提供整笔撤回 |

## 状态与操作

| 对象状态 | 页面操作 |
|---|---|
| 正常账户 | 详情、明细、充值 |
| 已冻结账户 | 详情、明细、查看限制 |
| 已关闭账户 | 详情、明细、重新开户；原账号和历史继续保留 |
| 未开户／未申请 | 申请开户 |
| 未开户／草稿 | 继续填写，可保存草稿或提交 |
| 未开户／审批中 | 查看申请、撤回 |
| 未开户／已退回、已撤回 | 重新提交 |

充值、扣款、金额冻结和余额明细的状态示例继续沿用[四页状态验收](门店四页列表状态示例与操作验收-20260915.md)，受当前账户状态限制对应动作。关闭账户只展示已完成或已结束的历史，不展示未完成扣款或仍冻结的金额记录。结算方式沿用当前门店档案的月结、单单结或待确认，不将预存账户当作结算方式，不增加授信场景。

## 验收与范围

13项浏览器综合检查通过，无脚本语法或浏览器运行错误。核对原7列、字体、内边距一致，列宽因状态文字自然适配，变化小于12像素；核对主列表及开户抽屉截图。检查脚本、结果及截图位于 `/tmp/caesar-store-step6/`。

本次是当前页可操作原型，申请及状态变化刷新后重置；不模拟财务审批、实际开户、流水匹配或跨页面回写。已有状态示例用于展示差异，不是完整可结算账簿，不做逐笔余额回算。整户冻结示例体现本例限制；具体限制组合由财务管理，不作为所有账户的统一政策。未明确的结算规则仍为待确认，不新增业务确认事项。

唯一下一步：待用户核对本页后，按后续指令处理财务预存账户页 `merchant/finance/finance-predeposit.html` 的最小调整。
