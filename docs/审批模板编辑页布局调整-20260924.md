# 审批模板编辑页布局调整

### 2026-09-24 指定成员空框处理

本步修复审批／抄送节点指定成员在候选人为零时只显示边框的问题。候选仍按模板适用公司和有效任职筛选，抄送不要求审批权限；未选公司提示先到基础信息选择适用公司，已选公司无人员则提示维护员工任职（审批节点同时提示核对审批权限）。成员所属公司仅显示模板适用范围内公司。未放宽跨公司筛选或改动存储。修改 `shared/approval-config.js` 及局部空态CSS。未选公司→选择福建公司→抄送成员显示及保存专项检查通过，27组配置DOM、语法检查通过；截图具体模板数据不可由图片确定，浏览器视觉待复核。下一步复核用户模板适用公司及成员显示，原主工作线断点不变。


### 2026-09-24 流程分支汇合线断口修正

用户截图中的汇合横线断口来自分支左右各12px内边距：原footer底边只覆盖内容宽度，两个分支之间留下24px空白，且横线错误延伸至画布外侧。本步仅修改 `shared/approval-config.css`，footer横向补齐分支内边距，独立伪元素画汇合线，首尾分支分别从自身中心开始／结束，连接中间分支与后续主线；保留短分支自动补齐高度及编辑／只读共用结构。20组字段／分支DOM回归通过，差异检查通过；DOM不验证像素位置，浏览器视觉仍待复核。下一步复核截图所示只读双分支及多分支汇合，原主工作线断点不变。


### 2026-09-24 审批模板列表单行精简

按用户要求，本步仅调整集团审批配置中的模板列表：删除流程摘要列，保留模板名称、业务场景、适用公司、状态、操作五列；删除名称下版本／示例／修订提示及公司下部门辅助行。字段单行显示，长文本省略且保留悬停全文；固定操作列不变，模板列表最小宽度调整为1040px。模板配置、部门范围、历史版本数据保留在原编辑及版本入口。修改 `shared/approval-config.js` 和局部CSS，27组配置DOM回归及语法通过，浏览器视觉待复核。下一步复核集团列表单行与窄屏滚动，原工作线断点不变。


## 2026-09-24 审批配置迁至集团端部门组织（用户明确）

本步范围：集团审批管理员维护跨公司审批模板、流程、规则和审批职责人员，上游引用组织架构及员工任职，下游供业务申请采用；商户审批中心仅保留日常办理。菜单为集团端“部门组织 → 审批配置”，主页面 `admin/approval-config.html`，二级编辑页 `admin/approval-template-edit.html`。沿用既有会话数据及取人逻辑，不新增真实权限服务或跨端回写。

| 要求 | 原缺口 | 修改 | 验收 |
| --- | --- | --- | --- |
| 集团统一配置 | 配置混在商户审批列表 | 集团独立列表和编辑页，admin导航及菜单归属 | 菜单选中、新建／编辑／历史查看通过 |
| 配置与办理分开 | 商户导航与员工页提供配置入口 | 移除商户配置菜单及员工配置按钮，办理四视图保留 | 待审、发起、抄送、总览回归通过 |
| 页面内入口正确 | 返回及员工任职跳转仍指商户 | 返回／发布回集团列表，员工入口指集团员工账号；配置权限项移入后台角色的部门组织 | 配置与规则交互通过 |
| 旧链接可继续使用 | 历史入口带模板ID及版本 | 旧配置和编辑地址跳转集团页，保留查询参数及hash | 旧配置人员Tab、旧编辑版本参数检查通过 |

证据：26组配置、13组层级、18组规则、20组字段／分支DOM检查共77组通过；另菜单归属及两类旧链接目标检查通过（脚本模拟，非浏览器实际导航）；JS语法、差异检查通过。遗留：浏览器排版及实际跳转待复核，权限项仅原型表达。下一步复核集团端列表、编辑及返回；原审批工作线的逐节点办理断点保持，不因迁移宣称后端权限或流程闭环已实现。


### 2026-09-24 恢复审批人抽屉说明精简

用户明确要求恢复刚撤回的上一步，重新应用说明精简版：删除重复常驻解释，保留默认折叠且随选项联动的层级示例、必要异常提示和按勾选顺序审批提示；缺人选项恢复为“向上查找最近的有效部门负责人”，与实际取人规则一致。覆盖此前说明展示方案，不改业务规则。恢复后语法及13组层级DOM检查通过；视觉仍待复核，主工作线唯一下一步不变。


## 2026-09-24 审批人说明联动与间距

本步按用户截图，只优化审批管理员设置节点时的说明，不改审批取人或下游流转规则。`shared/approval-config.js` 将层级说明、示例与来源、单级／连续、计数方向、选中层级联动；部门起点、缺人处理、同层会签／或签各自配套说明。公司职责、单据负责人说明也与控件同组。`shared/approval-config.css` 将组内说明间距统一为6px，清除段落和折叠示例的额外外边距，保留层级两列等宽、总宽100%。

证据：13组组织及层级DOM、26组配置DOM、20组取人规则检查通过，含新增方向／层级／示例／缺人策略说明联动检查；JS语法检查通过。实际浏览器视觉尚未验收。遗留为刷新页面核对视觉间距；主工作线唯一下一步保持原记录，本步不扩大流程范围。

## 最新调整：审批人来源

按用户确认方案，将“找人方式”改为“审批人来源”（抄送节点为抄送人来源），选项为指定成员、部门负责人、公司审批人员、单据负责人。部门来源下选择本部门／上级部门，明确按单据业务责任部门取人；仍沿用原部门及上级部门取人规则，旧节点保存复开不丢层级。公司来源下选择审批职责，按付款公司／合同主体公司／业务责任公司展示对应说明，人员仍在审批人员 Tab 维护，职责不等于岗位或权限角色。

单据负责人仅向已有明确来源字段的场景开放：产品发布、价格调整、代理入库及上线使用产品负责人；团期开排及调整使用团期负责人。依据为现有产品列表／详情、代理包装和团期资料中的 owner／负责人展示，不建立跨页数据读取。其他事项不新增该选项；历史已有但无明确来源的节点保留原配置并提示改选，保存及发布时阻止继续使用，不悄悄换人。原型试跑仍通过本次申请上下文中的负责人确定人员，正式映射与实际在途接入仍留原第二批。

本轮修改审批专用模型及交互脚本，101 组既有模型／DOM 检查通过；另核对层级切换、切换来源后保留层级、公司职责说明、保存复开、可用场景、旧节点提示及历史只读，均通过。语法、差异和财务执行清单检查通过。下一步在真实页面打开设置审批人复核显示与选项，浏览器视觉仍未验。

## 最新调整：审批内容简化为勾选清单

用户明确要求审批内容与其他表单 Tab 等宽，直接列出提交资料字段，必显项默认勾选置灰，其他项用复选框选择，不要调整、移除和分组设置。本次已统一使用 `ac-editor-form`（含内边距最大 1008px、居中），字段清单桌面两列、窄屏一列；取消后字段仍留在清单。移除选择字段、分组管理、位置调整及对应抽屉代码，也移除空值和补充资料必填的复杂配置入口；补充说明／附件纳入勾选清单，已有必填要求保留锁定，底层既有分组及空值数据不清除。

付款申请按 `finance-payment-apply.html` 的申请概览及金额资料，展开 22 项字段，含申请单号、类型、付款主体、申请人、日期、付款对象、来源单据、成本来源、付款依据、节点、计划付款日期、金额、币种、汇率、本币金额、可申请金额、本次上限、预付余额、冲抵及备注；另含两项补充资料。各事项仍按自己的字段目录显示，不混入付款执行结果。必显为本次原型默认展示要求，不改变来源表单填写校验，正式财务必显清单仍需业务确认。编辑旧模板时补齐当前必显字段至编辑副本，不写回既有发布版本。

验证：37 组模型、26 组配置 DOM、按新交互修订的 20 组字段／流程 DOM、18 组规则 DOM，共 101 组通过；付款字段来源核对、必显锁定、可选保存复开、历史只读、CSS 解析及语法检查通过。未补记浏览器视觉。下一步仅复核清单宽度、勾选状态及窄屏展示，旧分组编辑和预览方案不恢复。

同日最新范围调整：用户明确“模板信息”仅删分区标题，模板名称和业务场景保留；“审批详情预览”整个模块删除。已移除右侧预览、放大入口、抽屉及专用预览代码／样式，审批内容恢复单栏，保留字段与分组配置及补充资料设置。模拟办理的审批记录仍保留，样式改用自身名称。既有配置 26 组、按删除范围修订的字段／流程 23 组，共 49 组 DOM 回归通过；不再将已移除的页面预览列为验收项。下一动作复核基础信息仅去标题、审批内容无预览及空列。下方左右预览内容为前序实施记录，已被本次用户指令覆盖。

同日 Tab 滚动条修正：用户截图出现纵向滚动条，定位到本轮新增的 `overflow-x:auto` 会使另一轴形成自动滚动容器，与公共按钮百分比最小高度及下划线外伸共同产生溢出。已取消 Tab 滚动容器、使用明确按钮最小高度并将下划线收在内部；640px 以下四项等宽排列。仅改专用 CSS，解析和差异检查通过，实际显示仍待刷新复核；此前 DOM 交互通过不代表视觉已通过。

## 页面任务与范围

审批管理员从审批配置进入模板编辑，维护业务场景、公司部门、展示内容、流程及规则，保存草稿或发布供后续申请采用。本轮只调整编辑工作区和直接预览，保留既有审批逻辑、发布检查、历史只读和在途申请边界，不接入来源业务或真实审批执行。

## 本次调整

| 区域 | 原问题 | 实施结果 |
| --- | --- | --- |
| 工作区 | 页头及步骤条满宽，正文限制 1216px 靠左，右侧断开 | 页头、Tab、正文左右边界统一；Tab 与正文连为一个工作区 |
| 配置入口 | 四个数字大按钮与上下步同时存在 | 标准下划线 Tab：基础信息、审批内容、审批流程、审批规则；移除上下步按钮，切换保留未保存内容 |
| 基础信息 | 宽表单混排，全部部门也显示包含下级 | 分模板信息、适用范围；表单内部适度限宽居中，模板名称／场景双列；全部／指定部门单选，仅指定时展示部门及包含下级 |
| 审批内容 | 字段调整与预览分离 | 左侧分组与字段，右侧同步展示示例；可放大预览，窄屏上下排列；示例选项不计入未保存修改 |
| 审批流程 | 受窄正文宽度限制，试跑在全局页头 | 流程占工作区宽度，画布内部滚动；试跑放流程工具栏 |
| 审批规则 | 规则标题、设置项排列不统一 | 左侧分类标题、右侧设置，轻量分隔，窄屏纵向排列 |
| 保存发布 | 上下步与全局操作分散 | 顶部保留唯一保存草稿／发布入口，发布沿用全模板校验 |

## 修改与核验

- 修改 `shared/approval-config.js`、`shared/approval-config.css`。沿用公共 Tab、表单控件及按钮样式，移除旧步骤条与窄正文规则，未改导航。
- 两份既有 DOM 检查只调整入口：部门从下拉改为单选；模拟办理先进入流程 Tab 再试跑。
- 26＋25＋18 共 69 组既有 DOM 交互检查通过，覆盖草稿、发布、历史版本、节点、条件、内容与审批规则。
- 一次性核对工作区／Tab 结构、部门显隐、未保存内容跨 Tab 保留、规则保存、试跑位置、字段及补充资料的预览更新、示例切换不标记修改、全部资料保留；通过。
- JavaScript 语法、专用 CSS 解析、差异格式和财务执行清单检查通过；原 8 批／36 项／6 场景保持。

上述为结构和交互核验，不替代浏览器视觉。既有浏览器预览限制未绕过；实际桌面／窄屏排版、画布滚动及预览阅读体验仍待复核。

下一步：复核真实页面四个 Tab 的对齐、表单宽度、左右预览及窄屏布局；原第二批日常审批接入仍按原断点保留。

## 2026-09-24 设置审批人来源合并与层级同行

用户要求将“找人方式”并入“审批人来源”，并按企微截图把计数方向和指定层级放在同一行。已在 `shared/approval-config.js` 把来源直接列为指定成员、指定上级、连续多级上级、指定部门负责人、连续多级部门负责人、公司审批人员，以及适用场景的单据负责人。删除独立找人方式及计数设置折叠；指定层级／审批终点下直接并排显示方向和层级。CSS仅调整审批抽屉局部同行布局。

| 要求 | 原缺口 | 修改 | 验收 |
| --- | --- | --- | --- |
| 来源一次选完 | 来源与找人方式分成两处 | 单级／连续直接纳入来源下拉 | 保存复开连续来源正确；原成员、职责和单据来源保留 |
| 方向与层级同一行 | 方向藏在折叠区域 | 同行双下拉，无计数折叠 | DOM同容器、层级设置及试跑回归 |
| 不改变原流程 | 旧节点保留既有来源与层级结构 | 只做UI选项映射，同一类单级／连续切换保留层级参数 | 12组层级DOM、26组配置DOM、20组层级规则共58组通过 |

脚本语法及差异检查通过；浏览器视觉未验，不将DOM视为截图验收。本次局部调整已完成，下一步仍为组织申请接入发布模板及逐节点办理，其他未完范围保持。

2026-09-24 截图追加：计数方向与层级选择改为两列等宽，所在行宽度100%，中间8px间距，取消136px／220px固定列宽；仅调整局部CSS，CSS解析及差异检查通过，实际浏览器视觉待复核。
